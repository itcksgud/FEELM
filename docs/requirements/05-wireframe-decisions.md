# 와이어프레임 충돌 검토 및 결정 기록

- 검토 대상: `FEELM UI Mockups 찐최종 (1).html`
- 검토일: 2026-08-27
- 최종 갱신일: 2026-08-27
- 결정 이력: 1·2차 누적 결정 2026-08-26(Notion), 3차 결정 2026-08-27
- 이력 한계: 1차에서 2차로 바뀐 상세 내역은 확인할 수 없어 두 차수를 누적 기준선으로만 기록한다.
- 목적: 와이어프레임과 요구사항 정의서의 충돌 사항 및 `04-open-questions.md`에 대한 팀 답변을 공유한다.
- 개발 범위: 1차 MVP와 2차 MVP 전체를 유지한다. 누락된 2차 MVP 화면은 범위에서 제외하지 않고 추후 와이어프레임에 추가한다.

## 1. 와이어프레임 충돌 사항에 대한 답변

| ID | 충돌 사항 | 팀 답변·결정 | 상태 | 후속 조치 |
|---|---|---|---|---|
| C-01 | 프로필의 `본 영화 128 / 평가 96 / 취향 필름 13컷`이 프레임 정의와 맞지 않음 | 취향 필름은 사용자의 **전체 프레임 모음**으로 본다. 숫자 또는 화면 정의를 수정한다. | 수정 필요 | `본 영화`는 감상 확인 수, `평가`는 현재 Rating 수, `필름 컷`은 현재 Rating으로 생성된 Frame 수로 구분한다. Rating 삭제 시 Frame도 삭제되므로 평가 수와 필름 컷 수는 일치해야 한다. |
| C-02 | `평가 안 남긴 영화`가 이미 감상한 영화로 표시되지만, 기존 문서는 감상 확인과 평가가 함께 끝나야 감상 완료로 정의함 | 감상 확인 후 평가는 나중에 남길 수 있다. 평가 전은 `WATCHED_UNRATED`, 평가 후는 `RATED`다. | 확정 | Frame·Popcorn은 최초 평가 후 생성하고 Rating 삭제 시 함께 삭제한다. 감상 이력은 유지해 추천 후보에서는 계속 제외한다. |
| C-03 | 와이어프레임은 월간 리포트, FR-23은 반기 리포트 | **반기 리포트**가 맞다. | 확정 | 월별 리포트 문구·목록·월말 발급 안내를 반기 기준으로 수정한다. |
| C-04 | 온보딩 UI는 포스터 거리값을 사용하지만 데이터 정의는 좋아요/싫어요 | 최종 저장값은 **좋아요/싫어요**다. UI에 선택 상태를 표시하고 미선택은 건너뛰기로 처리한다. | 확정 | 픽셀 거리가 아닌 명시적 선택 영역·버튼을 기준으로 판정하며 건너뛴 카드는 저장하지 않고 새 후보를 제시한다. 온보딩 값은 일반 Rating과 분리한다. |
| C-05 | 팝콘 맛이 와이어프레임에는 6개, 요구사항에는 8개 예정 | 팝콘 맛은 **8개로 구성하는 방향**으로 결정한다. 실제 데이터 분석 후 최종 맛 목록과 매핑을 확정한다. | 방향 확정 | 데이터 분석 후 8개 맛의 명칭과 장르→맛 매핑표를 확정한다. |
| C-06 | `그룹 모드`와 `파티 모드`가 혼용됨 | UI·문서·API·코드에서 **파티(Party)**로 통일한다. | 확정 | 화면 제목과 안내 문구의 `그룹` 표현을 제거한다. |
| C-07 | 소셜 로그인, 계정 복구·변경·탈퇴, 프로필·알림 설정 등이 요구사항에 없음 | 해당 기능을 추가한다. 이메일 가입과 **구글·카카오·네이버** 소셜 가입·로그인을 지원한다. | 확정 | `AR-01.*`~`AR-04.*`로 추적한다. 동일 이메일의 기존 계정이 있으면 기존 로그인 수단으로 로그인한 뒤 설정에서 소셜 계정을 연결한다. |
| C-08 | 리포트 공유, 공개 취향 비교, 한줄평 목록, 트렌드 등 2차 MVP 화면이 없음 | 기능 범위는 유지하며 와이어프레임은 나중에 추가한다. | UI 보강 예정 | FR-24~FR-27 화면 및 상태를 추후 설계한다. API 설계 시 누락하지 않는다. |

## 2. `04-open-questions.md` 답변

| ID | 질문 | 팀 답변·결정 | 상태 | API·데이터 반영 사항 |
|---|---|---|---|---|
| Q-01 | 감상 확인 후 평가를 나중에 남길 수 있는가? | **나중에 남길 수 있다.** 평가 전은 `WATCHED_UNRATED`, 평가 후는 `RATED`다. | 확정 | 감상 확인만으로 Rating·Frame·Popcorn을 만들지 않고 최초 평가 후 Frame·Popcorn을 생성한다. |
| Q-02 | 필름은 전체 프레임인가, 최근·월간 프레임인가? | **전체 프레임**이다. | 확정 | `/me/film`은 전체 프레임을 기준으로 하며 기간별 화면이 필요하면 별도 필터·요약으로 제공한다. |
| Q-03 | 취향 리포트는 월간인가, 반기인가? | **반기**다. | 확정 | TasteReport의 기준 기간과 화면 문구를 반기로 통일한다. 최초 생성일·재생성 정책은 별도 결정이 필요하다. |
| Q-04 | 온보딩 드래그 거리를 어떤 값으로 저장하는가? | 명시적으로 선택한 **좋아요/싫어요**만 저장하고 미선택은 건너뛴다. | 확정 | `OnboardingPreference`는 `LIKE/DISLIKE`만 저장한다. UI 선택 영역·버튼으로 판정하고 미선택이면 새 후보를 제시한다. |
| Q-05 | 팝콘 맛은 6개인가 8개인가? | **8개로 구성할 예정**이며, 실제 데이터를 보고 맛 목록과 매핑을 확정한다. | 방향 확정 | 스키마는 `flavorId` 기반으로 설계하고 데이터 분석 후 8개 레코드와 매핑을 확정한다. |
| Q-06 | 평점 단위, 수정·삭제, 재감상 정책은 무엇인가? | **1점 단위**, 수정·삭제를 허용한다. 재감상은 별도로 관리하지 않는다. Rating 삭제 시 Frame·Popcorn도 삭제한다. | 3차 확정 | Rating은 정수 1~5다. 감상 이력은 유지하고 평가·Frame·Popcorn·취향 반영만 제거한다. 감상 이력이 있으므로 추천 후보에서는 제외한다. |
| Q-07 | 어떤 소셜 로그인을 구현하는가? | **구글·카카오·네이버** 소셜 가입·로그인을 구현한다. 같은 소셜 식별자는 한 서비스 사용자에게만 귀속한다. | 3차 명확화 | 소셜 신규 가입은 새 User와 SocialIdentity를 1:1로 함께 생성한다. 동일 이메일의 기존 User가 있으면 기존 로그인으로 로그인한 상태에서 설정 화면으로 연결한다. |
| Q-08 | 파티의 최대 인원과 초대·참여 방식은 무엇인가? | 최대 **4명**이며 앱 안에서 닉네임으로 초대한다. 초대는 만료되지 않고 거절 후 재초대할 수 있다. 활성 파티의 인원 변경 시 파티를 종료한다. | 확정 | 초대 수락·거절·재초대, 파티 종료와 사용자 차단 API가 필요하다. |
| Q-09 | 추천은 `맞춤 2 : 발견 1` 비율인가? | 기본은 **맞춤 2 + 발견 1**이다. 발견 추천 후보가 없으면 **맞춤 3**으로 채운다. 평가가 바뀌지 않으면 같은 추천 결과를 유지한다. | 범위 확정·후속 동작 미정 | 유형을 표시하고 추천 입력이 바뀔 때만 결과 버전을 갱신한다. 세 후보를 모두 원하지 않을 때의 동작은 3차 잔여 결정으로 둔다. |
| Q-10 | 구독하지 않은 OTT 영화를 제외하는가? | **제외하지 않는다.** | 확정 | OTT 구독 정보는 제공처 표시와 링크 정렬 등에 사용하되 추천 후보 제거 조건으로 사용하지 않는다. |
| Q-11 | 리포트 링크와 필름·팝콘 공개 범위는 무엇인가? | 공유 링크는 **앱 밖 사용자**를 위한 공개 링크이며 1개월 뒤 만료된다. 리포트 파일은 **PNG**로 객체 스토리지에 저장하고 단기 URL로 내려받는다. 공개 프로필·평가·필름·팝콘은 비회원도 볼 수 있다. | 범위 확정·내용 미정 | 공유 토큰·단기 다운로드 URL·프로필 공개 설정 API가 필요하다. 반기 리포트의 상세 지표·시각화는 미정이다. |
| Q-12 | `관심없음` 기능을 제공하는가? | 1·2차 누적 기준에서는 제외돼 있었으나, 세 추천이 모두 마음에 들지 않는 사용자의 다음 동작이 없어 3차에서 **재검토 항목**으로 돌린다. | 결정 필요 | 무변경 종료, 부정 신호 없는 `다른 추천 보기`, `관심없음` 재도입 중 선택한다. |

## 3. 1·2차 누적 핵심 결정 — 2026-08-26

2026-08-26 Notion에 적힌 1·2차 누적 기준이다. 1차에서 2차로 변경된 상세 이력은 확인되지 않으므로 아래 항목을 특정 차수의 신규·변경 사항으로 단정하지 않는다.

| 번호 | 결정 항목 | 1·2차 누적 답변·결정 | 상태 | 요구사항·구현 반영 |
|---|---|---|---|---|
| 1 | 평가 전 상태명과 Frame·Popcorn 생성 시점 | 사용자 표시는 **감상 완료 · 평가 대기**, 내부 상태는 `WATCHED_UNRATED`로 정의한다. Frame·Popcorn은 최초 평가가 저장되어 `RATED`가 된 뒤 생성한다. | 확정 | AR-06 |
| 2 | Rating 삭제 시 파생 데이터 처리 | Rating을 삭제해도 기존 Frame·Popcorn은 삭제하지 않는다. 같은 영화를 다시 평가하면 기존 Frame·Popcorn을 새 평가로 갱신하며 새 항목을 중복 생성하지 않는다. Rating이 없는 동안 수량 기반 집계는 유지하고, 평균 평점처럼 Rating이 필요한 계산에서는 해당 영화를 제외한다. | **3차 결정으로 대체** | AR-06 |
| 3 | 온보딩 좋아요/싫어요와 미선택 | UI에 좋아요·싫어요 선택 상태를 명확히 표시한다. 어느 쪽도 선택하지 않은 카드는 **건너뛰기**로 처리해 저장하지 않고 즉시 새 후보를 제시한다. 기기별 픽셀 거리 대신 UI의 명시적 선택 영역 진입·버튼 선택을 판정 기준으로 사용한다. | 확정 | AR-05 |
| 4 | 회원가입 이름·닉네임 | 실명 필드는 받지 않고 **닉네임만 필수**로 둔다. 닉네임은 공백 정리 및 대소문자 비구분 정규화 기준으로 전체 사용자 간 중복을 허용하지 않는다. | 확정 | AR-03 |
| 5 | 이메일·소셜 계정 중복 | 회원가입 시 이메일 인증을 필수로 하고, 정규화한 이메일에 유일 제약을 둬 중복 가입을 막는다. 동일 이메일의 기존 계정이 있으면 새 회원을 만들지 않고 로그인 또는 계정 연결을 안내한다. | 3차에서 표현 명확화 | AR-01, AR-02 |
| 6 | 8개 팝콘 맛과 장르 매핑 | 아직 정하지 않는다. 실제 데이터 분석 후 별도 결정한다. | 미정 | C-05, Q-05 유지 |
| 7 | 파티 수명주기와 차단 | 초대에는 만료 기간을 두지 않고, 거절 후 재초대를 허용한다. 모집이 끝나 결과가 생성된 `ACTIVE` 파티는 멤버 가입·탈퇴·강퇴·차단·탈퇴 등으로 인원 구성이 바뀌면 즉시 `ENDED`가 되며, 새 구성으로 새 파티를 만들어야 한다. 사용자 차단 기능을 추가하고 차단 관계에서는 검색·초대·프로필 상호작용을 제한한다. | 확정 | AR-08, AR-09 |
| 8 | 추천 페이지네이션·새로고침 | 현재 기본 추천 3개를 **교체**할지, 아래에 다음 후보를 **추가**할지, 이미 본 후보를 언제 다시 허용할지를 정하는 항목이다. | **3차에서 평가 변경 전 결과 유지로 확정** | AR-07.3 |
| 9 | 리포트 파일과 저장 | 전체 리포트 다운로드는 **PDF**, 공유 카드는 **PNG**로 생성한다. 파일은 비공개 S3 호환 객체 스토리지에 저장한다. | **3차 결정으로 대체** | AR-12 |
| 10 | 비회원 공개 범위 | 영화 검색·영화 상세는 로그인 없이 공개한다. 프로필 공개를 켠 사용자의 프로필·평가·필름·팝콘을 비회원도 볼 수 있게 하며 기본값은 비공개다. 취향 비교는 비교 주체가 필요하므로 로그인 회원만 사용할 수 있고, 대상 사용자는 프로필 공개 상태여야 한다. | 확정 | AR-10, AR-11 |
| 11 | 반기 리포트의 지표·시각화 | 아직 정하지 않는다. | 미정 | Q-03, Q-11 유지 |

### 3.1 1·2차 누적 기준에서 8번의 의미

1·2차 누적 기준에서는 현재 추천 3개를 새로고침으로 교체할지, 페이지네이션으로 이어 붙일지를 정하지 않았다. 3차 결정에서 **Rating 등 추천 입력이 바뀌지 않으면 기본 추천 3개를 유지**하기로 했으므로 단순 재조회용 페이지네이션·새로고침 정책은 더 이상 필요하지 않다.

## 4. 3차 핵심 결정 — 2026-08-27

| ID | 결정 항목 | 3차 답변·결정 | 상태 | 원자 요구사항 |
|---|---|---|---|---|
| T3-01 | Rating 삭제와 추천 제외 | Rating 삭제 시 그 평가로 생성된 Frame·Popcorn을 삭제하고 취향 집계에서도 제거한다. `ViewingRecord`는 `WATCHED_UNRATED`로 유지하며, 감상한 영화는 Rating 삭제 후에도 추천 후보에서 제외한다. | 확정 | AR-06.3~AR-06.5 |
| T3-02 | 소셜 가입과 기존 계정 연결 | 소셜 신규 가입은 새 User 하나와 SocialIdentity 하나를 함께 생성한다. 같은 `(provider, providerUserId)`는 한 User에만 연결된다. 동일 이메일의 기존 계정이 있으면 자동 통합하지 않고 사용자가 기존 로그인 수단으로 로그인한 뒤 설정에서 소셜 계정을 연결한다. | 확정 | AR-02.1~AR-02.4 |
| T3-03 | 차단 사용자 평가 노출 | 차단 관계의 로그인 사용자끼리는 프로필·필름·팝콘뿐 아니라 영화 상세 페이지의 평가·한줄평도 서로 볼 수 없다. | 확정 | AR-09.4~AR-09.5 |
| T3-04 | 리포트 파일·다운로드 | 저장·공유 리포트는 PNG 하나로 생성하고 비공개 객체 스토리지에 저장한다. 다운로드 요청 시 짧은 유효기간의 URL을 발급한다. PDF는 현재 범위에서 제외한다. | 확정 | AR-12.1~AR-12.4 |
| T3-05 | 추천 결과 갱신 조건 | Rating 생성·수정·삭제 등 추천 입력이 바뀌지 않으면 추천 3개와 결과 버전을 유지한다. 단순 재조회나 화면 새로고침으로 추천 영화를 교체하지 않는다. | 확정 | AR-07.3 |
| T3-06 | 추천 3개가 모두 마음에 들지 않는 경우 | 현재 `관심없음` 제외 정책을 유지하면 사용자는 평가를 바꾸기 전까지 새로운 추천을 받을 수 없다. 이것을 의도한 종료 상태로 둘지, `다른 추천 보기` 또는 `관심없음`을 도입할지 결정한다. | 결정 필요 | AR-07.4 |
| T3-07 | 추가 요구사항 분리 | `AR-01`~`AR-12`는 기능 묶음으로 유지하고 실제 구현·테스트 단위는 `AR-01.1` 형식의 원자 요구사항으로 분리한다. | 확정 | `06-additional-requirements.md` |

### 4.1 AR-02의 계정 관계

- 소셜 회원가입: `Google 계정 A → FEELM User 1`을 새로 만든다. 이 관계는 1:1 제약을 위반하지 않는다.
- 같은 Google 계정 A를 다른 FEELM User에 다시 연결할 수 없다.
- 한 FEELM User는 이메일 로그인과 Google·Kakao·Naver 식별자를 각각 연결할 수 있다. 따라서 전체 구조는 `User 1 : 로그인 수단 N`이지만, **각 소셜 식별자는 User 하나에만 속한다**.
- 기존 계정으로 로그인한다는 것은 비밀번호 또는 이미 연결된 다른 로그인 수단으로 본인 계정에 로그인하는 것이다. 로그인 후 설정에서 새 소셜 제공자를 연결한다.

### 4.2 추천 3개가 모두 마음에 들지 않을 때의 선택지

| 선택지 | 동작 | 장단점 |
|---|---|---|
| A. 그대로 종료 | Rating이 바뀔 때까지 같은 3개 유지 | 정책이 가장 단순하지만 사용자가 추천에서 막힌다. |
| B. `다른 추천 보기` | 부정 신호를 저장하지 않고 다음 후보 3개를 별도 요청 | 추천 모델을 오염시키지 않고 탐색은 계속할 수 있다. 기본 추천 결과 유지 정책과 별도의 탐색 동작으로 설계해야 한다. |
| C. `관심없음` | 영화별 부정 피드백을 저장하고 다음 후보 제공 | 사용자 의도가 명확하지만 피드백 저장·추천 제외·해제 정책이 추가된다. |

현재 구현 기준은 확정하지 않는다. 팀 답변 전까지 `AR-07.4`를 결정 필요 상태로 유지한다.

### 4.3 현재 남은 핵심 결정

| 우선순위 | 결정 항목 |
|---|---|
| P0 | 추천 3개가 모두 마음에 들지 않을 때 A/B/C 중 어떤 동작을 제공할지 |
| P1 | 8개 팝콘 맛의 최종 명칭과 장르 매핑 |
| P1 | 반기 취향 리포트에 포함할 지표와 시각화 |

## 5. 최신 용어 및 구현 원칙

- 감상 확인 후 평가 전 상태는 사용자에게 `감상 완료 · 평가 대기`, 내부에서는 `WATCHED_UNRATED`로 표현한다.
- 최초 Rating 저장 후 상태는 `RATED`이며, 이때 Frame·Popcorn을 생성한다.
- Rating을 삭제하면 해당 평가로 생성된 Frame·Popcorn도 삭제하고 취향 집계에서 제거한다. 감상 이력은 `WATCHED_UNRATED`로 유지한다.
- 감상 이력이 있는 영화는 Rating을 삭제해도 개인·발견 추천 후보에서 제외한다.
- 온보딩의 좋아요/싫어요는 일반 Rating에 저장하지 않는다.
- 온보딩 미선택은 저장하지 않는 건너뛰기이며 즉시 새 후보를 제시한다.
- 필름은 전체 프레임 모음이다.
- 리포트는 반기 단위다.
- 리포트 공유 링크는 서버에 저장하고 생성 후 1개월 뒤 만료하며, 외부 사용자가 접근할 수 있다.
- 저장·공유 리포트는 PNG로 생성하고 비공개 S3 호환 객체 스토리지에 저장한다. PDF는 현재 범위에서 제외한다.
- 객체 스토리지는 MinIO에 종속시키지 않고 S3 호환 인터페이스로 추상화한다. 로컬·자체 운영은 MinIO, 관리형 클라우드는 해당 클라우드 객체 스토리지를 기본 선택으로 한다.
- `Party`를 사용하고 `Group`과 혼용하지 않는다.
- 파티는 최대 4명이며 앱 안에서 닉네임으로 초대하고 수락한다.
- 파티 초대는 만료되지 않고 거절 후 재초대할 수 있다. 활성 파티의 인원 구성이 바뀌면 해당 파티를 종료한다.
- 사용자 차단 기능을 제공하며 차단 관계에서는 사용자 검색·파티 초대·프로필 상호작용과 영화 상세의 평가·한줄평 노출을 제한한다.
- 기본 추천 3개는 맞춤 2개와 발견 1개로 구성하고, 발견 후보가 없으면 맞춤 3개로 채운다. UI는 추천 유형을 표시한다.
- 추천 입력이 바뀌지 않으면 추천 3개와 결과 버전을 유지한다.
- 구독하지 않은 OTT의 영화도 추천 후보에서 제외하지 않는다.
- 팝콘 맛은 8개로 구성하되 명칭과 매핑은 데이터 분석 후 확정한다.
- 소셜 로그인은 구글·카카오·네이버를 지원한다.
- 회원가입은 닉네임과 인증된 이메일을 사용한다. 닉네임과 정규화 이메일은 중복을 허용하지 않는다.
- 동일 이메일의 기존 계정이 있으면 사용자가 기존 로그인 수단으로 로그인한 뒤 설정에서 소셜 계정을 연결한다.
- 영화 검색·상세와 공개 설정한 사용자의 프로필·평가·필름·팝콘은 비회원도 조회할 수 있다.
- `관심없음` 제외 결정은 재검토 중이다. 추천 3개가 모두 마음에 들지 않을 때의 동작은 아직 확정하지 않았다.
- 취향 리포트의 상세 구성은 아직 미정이다.

## 6. 저장 방식 선택 근거

- PNG 같은 바이너리 파일은 서비스 DB보다 객체 스토리지에 두고, DB에는 파일 메타데이터와 권한 상태를 둔다.
- MinIO는 S3 API를 제공하므로 애플리케이션이 S3 호환 인터페이스만 사용하면 운영 환경에 따라 MinIO 또는 관리형 S3 계열 저장소로 교체하기 쉽다.
- 객체는 기본 비공개로 두고, 사용자가 다운로드할 때만 짧은 만료의 URL을 발급한다. 리포트 공유 토큰의 1개월 유효기간과 실제 파일 다운로드 URL의 짧은 유효기간은 서로 다른 정책이다.

참고: [MinIO AIStor S3 API 안내](https://docs.min.io/aistor/developers/), [Amazon S3 presigned URL 안내](https://docs.aws.amazon.com/AmazonS3/latest/userguide/using-presigned-url.html)
