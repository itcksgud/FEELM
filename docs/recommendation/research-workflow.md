# 추천 연구의 실행·공유 규칙

새 연구 설계·코드 작성·실행·결과 검토·외부 공유 전에 읽는다. 경로는 저장소 루트 기준이다. 기존 AGENTS의 연구 규칙을 옮겼으며 사용자 지시와 독립 검토 의무를 유지한다. 현재 상태를 설명하는 읽기 전용 질문에는 현재 요약과 해당 근거만 읽는다.

## 6. Data and recommendation constraints

- MovieLens 사용자는 서비스 사용자와 별개다.
- MovieLens 평점은 상호작용 학습·오프라인 평가에, TMDB는 카탈로그·콘텐츠 특징에 사용한다.
- 미평가는 싫어요가 아니며, 누락 메타데이터와 OTT 미응답은 부정 선호 신호가 아니다.
- 현재 `ml-32m.zip`에는 Tag Genome이 없다. 자유 태그를 Tag Genome으로 취급하지 않는다.
- 한국 OTT 구독 비교는 `KR`의 `flatrate`를 구매·대여와 구분하고 스냅샷 시각을 저장한다.
- 추천 모델은 고정 split·candidate·seed와 기준선 비교 없이 채택하지 않는다.

세부 기준은 다음 문서를 따른다.

- `docs/research/movielens-tmdb-data-audit.md`
- `docs/research/movielens-recommendation-evaluation-design.md`

## 9. Recommendation experiment cycle

2026-09-08 최신 사용자 지시: **로컬에서 충분히 연구하고 결론을 정리한 뒤 필요한 내용을
Jira·GitLab에 기록한다.** 앞서 요청한 Jira 선등록 및 실험별 자동 게시 흐름을 대체한다.

현재 로컬 연구와 외부 게시 상태는 `docs/recommendation/active-experiment.md`를 먼저 확인한다.

1. **로컬 설계**: 사용자가 판단하려는 전체 질문을 기준으로 공식 요구사항의 확정·미확정
   사항을 구분하고, 기준선·비교 대상·입력·후보·
   사용자 분리·채점 기준·판정/중단 조건을 고정한다. 미평가 영화는 UNKNOWN으로 유지한다.
   Jira Key를 로컬 연구 시작 조건으로 삼지 않는다.
2. **실행 전 독립 검토**: 다른 세션 또는 별도 검토 에이전트가 설계와 실행 코드를 확인한다.
   필요한 수정은 반영하고 검토한 버전을 기록한 뒤 실행한다.
3. **실행 및 결과 검토**: 설정·버전·입력 출처·집계 결과를 로컬에 보존하고, 별도 검토자가
   계산과 데이터 누수, 결과가 뒷받침하는 주장의 범위를 검증한다.
4. **결론 정리**: 전체 질문에 대해 채택·유지·기각·보류와 판단 근거, 한계 및 다음 결정
   사항을 정리해 사용자에게 먼저 설명한다. 개별 실험 번호나 중간 결과의 완료를 전체 연구의
   결론 또는 외부 게시 시점으로 임의 간주하지 않는다.
   승자가 없거나 채점 자료가 부족한 결과도 명시하며, 결과를 보고 판정 기준을 바꾸지 않는다.
5. **결론 이후 공유**: 사용자가 공유할 결론과 범위를 정해 Jira·GitLab 작성을 요청하면,
   그때 팀 저장소 `S15P21E106/AGENTS.md`, `docs/jira/README.md`와 Git 규칙에 따라 기록한다.
   결론에 필요한 문서, 설정, 실행·검증에 필요한 코드와
   소형 결과를 팀의 현재 `pipeline` 구조에 정리한다. 대용량 모델·Parquet은 Git에서 제외하고
   외부 위치·해시·공유 상태와 재현 가능한 범위를 기록한다. 개인 실험의 모든 중간 기록을
   일괄 복사하지 않는다. Jira Key가 포함된 별도 작업 브랜치에서 커밋·Push·develop 대상 MR을
   만들고, Jira에 MR·결론·검증 결과를 연결한다.

- 로컬 설계·실험·검증의 독립 검토는 계속한다. 이를 위해 중간 Jira나 MR을 자동 생성하지 않는다.
- 이전의 상시 외부쓰기 승인, 과거 실험 문서의 Jira-first 순서와 인계 완료 조건을 새로운
  게시 승인으로 사용하지 않는다. 과거 결과·봉인은 당시 기록으로 보존한다.
- 연구의 완료 범위는 사용자가 판단하려는 질문까지 포함한다. 분류 기반 추천의 효과를
   판단하는 업무를 분류 구성·후보 공급 진단에서 끝내거나 추천 품질 검증을 임의로 다음 업무로 분리하지 않는다.
- 추후 Jira를 작성할 때 SP는 파일 수·실험 횟수가 아니라 전체 범위의 설계 복잡도·불확실성·
  검증 난이도로 산정한다. 실제 배포나 공통 계약 변경은 연구·공유 요청에 포함된 것으로 해석하지 않는다.
- MR 생성 후에는 공유 완료·리뷰 대기를 구분해 보고한다. 승인과 병합은 사람이 하고,
  Jira 완료 상태는 팀의 병합 후 완료 절차를 따른다. 병합된 브랜치는 다음 실험에 재사용하지 않는다.

## 10. 데이터 관계 분석의 현재 범위

2026-09-09 최신 사용자 지시: **데이터 관계를 파악할 때 8개 분류를 전제로 삼지 않는다.**

- 현재 연구 범위는 `docs/recommendation/analysis-scope.md`를 따른다. 관계 분석의 입력·표본
  선정·채점·결론을 팝콘 맛 개수, 특정 군집 수, K-means 또는 미리 만든 장르 묶음에 맞추지 않는다.
- 복수 장르·키워드·인물 등 원래 속성을 유지한다. 영화나 사용자를 사전 고정된 취향 유형
  하나에 강제로 배정하지 않는다. 비교를 위한 정보 묶음의 개수도 8로 고정하지 않는다.
- 분석 결과를 사용자에게 어떻게 표현할지, 분류·군집을 도입할지와 그 개수는 관계 분석 후
  별도 판단한다. 기존 제품 표현 구상을 현재 분석의 제약으로 되가져오지 않는다.
- 과거 실험의 고정 코드·설정·결과·검토 봉인은 당시 기록으로 보존하고 현재 방침과 구분한다.
  새 분석을 한다면 최신 범위로 설계·독립 검토를 수행한다. 이 범위 조정만으로 운영 API·DB를 바꾸지 않는다.
