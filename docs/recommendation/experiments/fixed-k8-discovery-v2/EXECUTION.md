# Fixed K8 discovery v2 — bounded execution contract

Status: DRAFT, local research only. 2026-09-13. No stage may run without an independent PASS for its exact code/config/contract/input hashes. This document becomes the execution contract when reviewed; it does not change service policy.

## Authority and question

The user asks for genuinely underexposed fine groups selected by personal content fit, TMDb count-adjusted quality prefixes, then a bounded number of predictions. Preserve the old top eight absolutely. The concept DESIGN SHA256 `5566bf99da165ffa83d273873a03549714b0910e08621237b54de3487bb1db84` and latest delegation supersede the v1 NDCG adoption gate. Existing v1 results, source worktrees, and seals are read-only. No commits, push, Jira/GitLab/Notion, deployment or external messages.

Main question: which frozen subgroup/representative/budget combinations supply underexposed candidates with acceptable technical approximation and evidence, and what do they cost? MovieLens future recall is a descriptive panel. It neither selects discovery policies nor imposes noninferiority. Human discovery satisfaction remains unmeasured.

## Input and time contract

- All 237,817 service rows, sorted by integer service_movie_id; validate one-to-one TMDb identity. Reuse v1 catalog mapping, original content preprocessing and contexts with exact pins. Audit original details and keywords archives; preserve raw field presence, null, type, finite/range and contradictions before scoring. No network download.
- Source archive names describe a collection range, not a per-film observation timestamp. Save every top-level timestamp-like field, tar member mtime and source path; report whether a trustworthy per-row fetched time exists. Never substitute tar mtime for fetched time.
- Current-catalog simulation: Released, valid release_date <= 2026-09-09, adult=false, video=false, supported frozen GKT content, and valid quality evidence. The entire classification includes excluded films. Current TMDb aggregates and old ML histories make this a development simulation, not historically leakage-free recommendation.
- Valid Q evidence: numeric finite R in (0,10], integer finite v>0, excluding booleans. R=0 with v>0 is AMBIGUOUS_ZERO_AVERAGE, v=0 with R>0 is CONTRADICTORY_ZERO_COUNT. Missing/null/type/range and zero votes retain separate flags; Q UNKNOWN rows stay classified but are absent from the main quality pool. No imputation of anomalies.
- C uses the frozen valid eligible reference set. Compare unweighted movie mean and vote-weighted mean, m=[100,300,1000]. The declared main Q is movie mean, m=300. This is an explicit robustness assumption, not a statistical optimum. Q=(vR+mC)/(v+m), ties v descending then service ID ascending. Sensitivity does not silently replace the main Q. raw-R and count-only are diagnostic controls. Q is neither a confidence bound nor a personal rating.
- Predictor's preexisting internal RH prior C=6.173088067675869,m=48 stays separate and frozen. Its raw-metadata adaptation and old-feature parity require their own review.

## Frozen geometry and hierarchy search

- v1 bundle SHA256 `cbdc51940b9f3fe9a295ae802e4af45009397af64231c6a79884e673d58ef937` supplies preprocessing, top centers, weights, IDs and names. Recompute and compare all 237,817 top assignments before use. Never refit top8 or preprocessing.
- Primary content x is exactly the existing normalized concatenation sqrt([.5,.35,.15]) * [normalized genre, keyword SVD64, lexical overview SVD48]. No crowd features. The G-only search space is a control on the same frozen v1 16 child assignments.
- Compare frozen v1 G-fixed2 assignments (16 children) and new GKT K=[2,8,32,64,128,256] PER TOP TASTE. Each new fit uses all supported films within its frozen parent. K-means Lloyd, seed=913+parent, n_init=3,max_iter=100, two BLAS threads. If supported rows or distinct vectors <K, use min(K,distinct) with explicit requested/actual K and alias; no minimum100 gate. Empty numerical clusters are retained as centers/IDs and marked if encountered. Under-supported/zero vectors are assigned deterministically but excluded from representative means.
- Final centers canonicalized once by lexicographic rounded-to12 coordinates, then fixed float64 elementwise squared-distance nearest, lowest ID exact ties. All saved arrays/model/source IDs versioned; add/delete/reorder/batch/reload/null/OOV must preserve existing identical-input top/child/group IDs and representative arrays. Additions assign against stored centers; deletions tombstone candidates without recentering.
- Representatives compared on the SAME hierarchy: equal supported-film mean (primary), unit-normalized mean, actual medoid approximation (supported film nearest the mean, lowest ID tie), and four fixed subcenters (KMeans up to4 distinct supported vectors) with logmeanexp(score/T)*T, T=.1, equal weights. This normalizes representative count; zero supports are flagged. Name/explanation exemplars are separate. Also record multi-representative dot-product count.
- First isolate representative effect on frozen16. Then compare all four on each GKT hierarchy. G-only on frozen16 and separate positive/negative profile on frozen16 mean are fixed controls. No arbitrary names imply coherent semantics; use source metadata and masked examples to review.

## Users, profile and E_u

- Reuse the270 development users; align90 development-selection to frozen final344 calibration users and180 development-check to final344 comparison users. The v1 roles are preserved only as historical audit. Prior reuse is explicit. Main cap10 includes only ACTUAL_HISTORY with a valid supported non-negligible direction. Evaluate cap30 and cap0 as diagnostic sensitivity on finalists; cap0 preserves full observed viewed exclusion.
- Read original capped contexts before service mapping. Reuse v1 audited full pre-origin viewed service IDs (reconstructed from128 original rating parts), pin its context and source audit. Main simulation history origin2023-01-01, future target horizon180d. No future target becomes a user-profile input and no new calibration is fit. The90 selection users' targets already informed the reused predictor's fixed calibration; the180 check users are disjoint from that calibration, but remain previously reused development data. Viewed and rated inputs are distinct. ML ratings proxy observed viewing; absence is not proof of not watching.
- Let rbar=(sum(r)+5*3.5)/(n+5), anchor=3+.5*(rbar-3), w=r-anchor. Main raw direction=sum(w*x)/(sum(abs(w))+5); rank with its unit direction. Save raw norm, support count, positive/negative counts, effective_n=(sum|w|)^2/sum(w²), confidence=effective_n/(effective_n+5) separately. Confidence is not claimed to survive unit normalization or independently improve ranking.
- All rated inputs determine rbar; only supported input vectors determine the vector sum/evidence. Two-branch control forms positive and negative weighted means separately, subtracts with lambda=1, then uses its direction, recording each branch support.
- Distinct states: ACTUAL_NO_HISTORY (raw past0), HIDDEN_CAP0 (raw past>0 cap0), UNMAPPED_INPUT (capped original input>0 mapped0), UNSUPPORTED_INPUT (mapped>0 supported0), CANCELLED_OR_TINY (raw norm<=1e-8), VALID. Synthetic exact cancellation vs 1e-12 tiny recorded separately in tests. No-vector states return no personalized discovery; never ID-sort and count as personalized.
- viewed_g counts ALL distinct observed viewed films by frozen child, including low ratings. Underseen requires count<=2 AND count/max(1,all mapped viewed)<=.2. E_u additionally requires at least one eligible unseen movie. Restrict E_u FIRST, then rank by profile-representative fit. Top-taste exposure never excludes all its children. No mixing fit with underseen score.

## Retrieval, controls and cost

- Budgets B=[50,100,200], per-group unseen quality prefix quota=[10,25]. Initial group batch P=ceil(B/quota). Visit E_u in decreasing representative score, lower group ID ties. Take the group's unseen prefix up to quota; continue to next legal group until B or all E_u exhausted. P is initial probe count, never a reason for global fill. Each movie is scored at most once. Return up to10 final movies. Exhaustion returns short/empty. Hard invariants: E_u-outside0, viewed0, duplicate0, globalfill0, prediction rows<=B.
- Same E_u, eligibility, seen filter and per-group Q prefix DIRECT-PREFIX reference: compute profile dot all actual unseen prefix movies, rank groups by mean prefix similarity, then apply IDENTICAL visit/quota/B. This isolates representative ranking approximation. Also store top5-prefix-mean ranks as a diagnostic, not interchangeable with mean. Candidate overlap and content mean difference are signed; approximation need not always be worse.
- E_u ALL-CONTENT reference lifts the Q-prefix restriction and takes topB per-movie content similarities, then the same predictor. Difference includes representative plus prefix constraints. E_u-FLAT-Q takes global Q topB within E_u without group probes. RANDOM-E_u uses seeds913/914/915 and same quotas/B. GLOBAL-FLAT-Q and GLOBAL-FULL-PREDICTOR do not impose E_u and are clearly separate opportunity-cost references. E_u-FULL-PREDICTOR is the within-constraint model reference. Neither is human truth.
- All final prediction/rank comparisons use the same selected predictor and candidate eligibility. Full-content/predictor references are costlier and timings say so. Timed candidate paths actually call the model on retrieved rows, not slice previously saved full scores. Record initialization/index preparation and warm request CPU/wall time, prediction calls/rows, content comparisons and representative comparisons. Rotate policy order by user. Warmup excluded, two threads.
- Save per-user candidate indices, source groups, group rank, within-group unseen Q rank, final IDs/scores/model branches and raw support counts. Record fixed-prefix versus actual viewed-excluded prefix mismatch, unsupported rates, mean and worst-tail losses, duplicate centers/small groups/empty eligible groups.

## Predictor comparison and selection (separate from discovery objective)

- Implement exact service-axis RH230 metadata and the existing selected calibrated ACTUAL_ALS -> GBT120_s339 fallback. Do not relabel the old toy ALS/ridge as the selected model. Validate old-axis identical-input feature/prediction parity and fresh unseen IDs before real evaluation; record unavoidable source-version differences separately from feature-code parity.
- Finite same-candidate alternatives: original router; support-shrink20; support-shrink100 (a=n_train/(n_train+tau), interpolate calibrated ALS with calibrated GBT only where ACTUAL_ALS exists); min20 (ALS only for n_train>=20, otherwise calibrated GBT). Q is never blended into personal predictions. Rank with the original affine scores without clipping; clip only the rating-error readout to [.5,5], preserving source model parity. Q-order-preserved is a no-personal-rerank diagnostic with predicted values still evaluated separately.
- Use actual-vector selection users' observed targets for MSE/MAE and calibration by train support, input support, language/year; this is predictor diagnosis, not discovery satisfaction. Select strongest shrink among [shrink100,shrink20,min20] satisfying observed global MSE<=1.02*original and low-support<=20 MSE<=1.05*original if>=30 rows, plus at least20% reduction in low-support ALS Top1 count on frozen16 mean B100/quota25 candidates when original has any. No evidence for a stratum -> report underdetermined, never manufacture a PASS. If no alternative qualifies keep original as research comparator and mark predictor-risk HOLD. Freeze selected predictor before hierarchy ranking/check users. No new model training or calibration fitting.

## Finite execution and decisions

1. Prepare/input audit and frozen geometry: exact reviewed code; no user-target-based selection.
2. Fit six new GKT hierarchies (up to48 parent fits) plus bounded four-representative construction (up to the total groups; capped4), no retry for pretty clusters. Old16 retained. Compute Q6-way sensitivity, support/duplicates/coherence/prefix tables.
3. Predictor alternatives on selection users, then freeze route. Selection-policy sweep: 7 hierarchies*4 reps*3B*2quotas=168 plus the fixed G-only/two-branch controls and matched baselines. Invalid-profile users saved but excluded from personalized denominator. Target NDCG/recall columns cannot enter selection code.
4. Technical candidate gates on selection users: all hard invariants; returned10 for>=95% valid users; mean DIRECT-PREFIX overlap@B>=.60; p95 absolute mean-content gap<=.10 on unit-vector similarity; maximum signed loss<=.25; p95 warm request<=250ms. These are explicit local engineering tolerances, not human quality evidence. Low TMDb v<=20 Top1 share>10% or ML support<=20 Top1 share>10% is a risk flag and invokes the predeclared minimum-TMDb21 sensitivity, not silent removal or automatic semantic failure.
5. Keep Pareto feasible policies across lower predicted-row cost/latency and lower direct-prefix mean loss. Choose minimal B then minimal totalK then mean/norm/medoid/multi4 order then quota25 for a review candidate; also keep best approximation and K128/K256 technical tradeoffs regardless of gate outcome. If no feasible, HOLD with explicit nearest tradeoffs. Run the chosen plus per-K best and all reference controls on180 check users; do not reselect on check outputs. Run cap30/cap0 and minimumTMDb21 sensitivity for selected finalist; same E_u exhaustion, no global fill.
6. Current-catalog observed rating/negative/UNKNOWN/recall panels, not success labels. Report language (ko/en/other/missing), production KR, year<=1999/2000s/2010s/2020-22/2023/2024-26/missing; TMDb0/1-5/6-20/21-100/101+ and ML train0/1-5/6-20/21-100/101+ separately. Count Top1/slots/unique films/users and candidate->Top1 upward jumps. Report raw count and conditional share, denominators, worst user/group, common fixed8 exposure, genre/keyword overlap with observed viewed and collection repeat concentration. Unknown ratings remain UNKNOWN; short lists use returned denominator plus requested-slot supply.
7. Freeze selected subgroup/representatives and run all-catalog invariance plus mutation stress: append raw-source duplicates with fresh IDs/null/OOV, remove representative/existing movies, reverse/rebatch/reload. These are mutation fixtures, not held-out movies: all original movies enter fitting. Recompute current candidate-prefix drift without updating representatives. User-swap/shuffle/sign-reversal must produce the specified change only in nondegenerate synthetic fixtures; actual user selection/rank changes are distributions, with no forced-change assertion. Independent prefix evidence uses unused-in-v2-selection check-user inputs and masked source examples, not a claim of movie holdout.
8. Independent exact numerical/leakage/claim audit; fix real defects in a new run version, preserving earlier run. Maximum2 correction runs after the initial run. Criteria remain fixed except an explicit reviewed bug in the criterion itself, with old/new decision shown. Performance disappointment alone never opens another tuning round. If still failing after2 corrections, deliver rejection/HOLD and concrete limitations.

Resources: CPU only, two threads, max process RSS8GiB, minimum available host2GiB, max stage90min, max overall local compute6hours, max output10GiB. Sample no more than2,048 films per group for fixed multi-representative fitting using frozen service-ID hash if needed; its full-group support remains reported. Stop on resource guard and report partial scope; do not replace full service catalog silently. Large artifacts ignored; exact reproducibility code/config/small audit/report kept local.

## Required delivery

Execution/config and review fingerprints; raw quality evidence and time audit; 237,817 frozen assignments; hierarchy/representative bundle and append/tombstone inference; actual recommendation code with no-vector/short/exhausted states; all policies and rejected conditions; real candidate/final support/prediction cost/loss audits; Korean-familiar film examples with service/TMDb IDs, R/v/Q, group/source reasons and actual user input connection. Independent AI metadata review finds explanation errors, not Korean satisfaction. Final state distinguishes technical review candidate, tradeoff, predictor-risk HOLD and unmeasured human value.

Pre-execution user steering: stronger count adjustment for discovery. Before any new numerical run, main m changed from100 to300 and sensitivity from10/30/100/300 to100/300/1000. This does not change the separately frozen predictor RH prior m48. No results informed this amendment.

## Pre-execution review clarifications (normative)

- DIRECT-PREFIX overlap = intersection(candidate,reference)/len(reference), separate from len(candidate)/B and len(reference)/B supply. If either list empty, content gap/overlap are undefined, never zero-loss. All VALID-user, comparable-user and full-supply-user counts accompany aggregates. Gates require >=95% comparable users as well as >=95% returned10; undefined pairs do not pass silently. Mean-content gap = reference mean(p·x) minus candidate mean(p·x); signed positive loss, absolute error and positive-part loss all reported. Gate p95 uses absolute gap; worst uses maximum signed positive loss. Pareto approximation cost is mean absolute gap, not signed gap that would reward incomparable upside.
- Predictor primary MSE is equal-user macro mean of each user's observed-target MSE. Low-support MSE is equal-user macro among users with eligible target rows n_train<=20. Also report row-micro MSE/MAE; a predictor gate requires at least30 low-support target rows AND10 users. Otherwise model choice remains original research comparator and reliability alternative is UNDERDETERMINED, not passed. All alternative low-support Top1 exposure uses invariant movie cohort (original ACTUAL_ALS support exists for that user AND movie train_count<=20), regardless of the alternative branch name. Overall low-support movie exposure is additionally reported across GBT fallback too. No zero denominator is a percent improvement.
- Each hierarchy's best is among feasible policies if any; otherwise among all with defined comparisons and an explicit gate-failure flag. Order: lowest mean absolute DIRECT-PREFIX gap, then smallest B, then mean/norm/medoid/multi4, then quota25 before10. Overall Pareto is joint minimization of actual mean predictor rows, p95 latency and mean absolute gap with at least one strict improvement; overall candidate priority remains smallest B, totalK, representative order, quota25, then stable policy ID. The per-K best plus K128 andK256 rows are retained even if none feasible. A candidate lacking comparisons cannot be chosen; total failure yields HOLD.
- Frozen16 controls G-only mean and two-branch GKT mean use B100/quota25. Add one Q-weighted mean representative control on frozen16 at B100/quota25, with weights main frozen Q on quality-valid supported films and normalized positive weights. Its mean is not unit-normalized. This is a bounded diagnostic, excluded from the168 primary-policy selection grid, and tests the concept's quality-weighted representative directly.
- Multi4 uses equally weighted mode representatives deliberately to test minority modes; logmeanexp normalizes count but does not remove spread or mode weighting bias. Save actual representative count, fit-sample and full-member nearest-mode masses, representative pairwise spread, and average-vs-mode ranking mismatch. Duplicate/zero modes remain visible. No semantic coherence claim follows from self-space approximation alone.

### Correction run r2, before any new clustering/prediction

The parallel independent audit found57 of the v1 check180 overlap the reused hybrid345 calibration users (33 overlap in v1 selection90). Run r1 completed input preparation only, 59.04s, no new clustering, predictions or target-based choice. Preserve its inputs, result seal and exact code/contract snapshot. Run r2 corrects roles using final344/roles.csv SHA256 `466b7cede9cb2d67f2bd7fca4fcdada770943bdd9d5924c35ccfdbe6a4cf2cc9`, calibration90=>selection andcomparison180=>check. This is a role/leakage-contract defect correction, not outcome-driven tuning. One of at most two correction runs used. All prior Q, retrieval and approximation criteria remain unchanged. Preparation must be rerun under independent exact-version review.
