## FILE: AGENTS.md
# FEELM standalone agent contract

## 1. Repository scope

- 이 저장소는 개인용 FEELM 독립 프로젝트다. 팀 GitLab 기준 저장소 `S15P21E106`과 작업·브랜치·계약을 섞지 않는다.
- 현재 작업 위치는 별도 worktree와 `project/standalone-feelm` 브랜치다.
- 사용자가 명시적으로 요청하지 않으면 commit, push, 배포, 외부 계정 변경을 하지 않는다.
- 추천 연구는 9절에 따라 로컬에서 충분히 검증하고 결론을 정리한다. Jira 선등록과 실험별 자동 GitLab 게시에 대한 이전 상시 지시는 철회됐다.
- 저장소 밖의 파일은 참고 원본으로만 읽는다. 프로젝트가 의존하는 자료는 출처와 버전을 기록한 뒤 저장소 안의 계약 문서로 정규화한다.

## 2. Specification authority

충돌이 있으면 다음 순서로 판단한다.

1. 현재 사용자의 명시적 지시
2. `APPROVED` 상태의 제품 범위·업무 규칙 문서
3. `docs/api/openapi.yaml`과 DB Schema·migration
4. 화면 계약과 acceptance criteria
5. 아키텍처·추천·데이터 계약과 승인된 ADR
6. 요구사항 원문 전사와 결정 기록
7. 연구·비교·발표 문서

하위 문서가 상위 계약과 다르면 코드를 기준으로 계약을 바꾸지 않는다. 불일치를 기록하고 상위 계약을 먼저 수정한다.

## 3. Document status

구현 판단에 쓰는 문서는 머리말에 다음 상태 중 하나를 표시한다.

- `DRAFT`: 탐색용이며 구현 계약이 아니다.
- `APPROVED`: 구현 기준으로 사용할 수 있다.
- `SUPERSEDED`: 새 기준 문서 링크를 남기고 사용하지 않는다.

현재 `docs/requirements/00-source.md`와 그 파생 분석은 요구사항 출처지만 그 자체가 최종 구현 계약은 아니다. 확정 계약 세트와 자율개발 준비도는 `docs/planning/llm-autonomous-development-readiness.md`에서 추적한다.

## 4. Implementation rule

기능 구현 전에 다음 연결이 존재하는지 확인한다.

```text
Requirement ID
→ Screen/state or internal use case
→ API operation
→ Entity/source of truth
→ Acceptance criterion
→ Automated test
```

- 위 연결 중 하나가 없으면 먼저 계약을 보완한다.
- 제품 의미를 바꾸는 미정 사항을 임의로 결정하지 않는다.
- 되돌리기 쉬운 기술 선택은 ADR에 가정·대안·검증 방법을 기록하고 진행할 수 있다.
- 한 작업은 하나의 수직 기능 또는 하나의 인프라 목적만 포함한다.
- API·DB·이벤트·추천 출력 변경은 소비자와 migration 영향을 함께 수정한다.

## 5. Definition of done

완료라고 보고하려면 최소한 다음을 충족한다.

- 연결된 Requirement와 acceptance criterion이 있다.
- 정상·빈 상태·권한 오류·외부 장애 중 관련 상태를 처리한다.
- 단위 또는 계약·통합 테스트가 있으며 로컬 명령으로 통과한다.
- OpenAPI, migration, 예시 데이터와 구현이 일치한다.
- 새 환경 변수는 `.env.example`과 로컬 실행 문서에 설명한다.
- 비밀값, 원본 대용량 데이터, 생성 결과를 commit 대상에 넣지 않는다.
- 관련 문서와 추적 매트릭스를 같은 변경에서 갱신한다.

## 6. Data and recommendation constraints

- MovieLens 사용자는 서비스 사용자와 별개다.
- MovieLens 평점은 상호작용 학습·오프라인 평가에, TMDB는 카탈로그·콘텐츠 특징에 사용한다.
- 미평가는 싫어요가 아니며, 누락 메타데이터와 OTT 미응답은 부정 선호 신호가 아니다.
- 현재 `ml-32m.zip`에는 Tag Genome이 없다. 자유 태그를 Tag Genome으로 취급하지 않는다.
- 한국 OTT 구독 비교는 `KR`의 `flatrate`를 구매·대여와 구분하고 스냅샷 시각을 저장한다.
- 추천 모델은 고정 split·candidate·seed와 기준선 비교 없이 채택하지 않는다.

세부 기준은 다음 문서를 따른다.

- `docs/research/movielens-tmdb-data-audit.md`
- `docs/research/movielens-recommendation-evaluation-design.md`

## 7. Secrets and external services

- 실제 토큰은 `.env.local` 또는 실행 환경 secret으로만 주입한다.
- 로그, 문서, 테스트 fixture, 명령 출력에 토큰을 노출하지 않는다.
- OAuth, 이메일 발송, 배포 계정처럼 사용자의 외부 자격증명이 없으면 로컬 fake와 adapter 경계까지만 구현하고 실제 연동 완료로 표현하지 않는다.
- TMDB, MovieLens, JustWatch의 라이선스와 attribution 조건을 유지한다.

## 8. Verification and handoff

- 로컬 실행·검증 명령은 `docs/runbook/local-development.md`가 생성된 이후 그 문서를 단일 기준으로 사용한다.
- 자율개발 가능 여부는 새 대화·새 작업 환경에서 기존 대화 이력 없이 수행하는 blind handoff로 검증한다.
- 검증 실패를 프롬프트 추가로 숨기지 말고 누락된 계약, fixture, 명령 또는 acceptance criterion으로 환원해 저장소에 반영한다.

## 9. Recommendation experiment cycle

2026-09-08 최신 사용자 지시: **로컬에서 충분히 연구하고 결론을 정리한 뒤 필요한 내용을
Jira·GitLab에 기록한다.** 앞서 요청한 Jira 선등록 및 실험별 자동 게시 흐름을 대체한다.

현재 로컬 연구와 외부 게시 상태는 `docs/recommendation/active-experiment.md`를 먼저 확인한다.

1. **로컬 설계**: 사용자가 판단하려는 전체 질문을 기준으로 공식 요구사항의 확정·미확정
   사항을 구분하고, 기준선·비교 대상·입력·후보·
   사용자 분리·채점 기준·판정/중단 조건을 고정한다. 미평가 영화는 UNKNOWN으로 유지한다.
   Jira Key를 로컬 연구 시작 조건으로 삼지 않는다.
2. **실행 전 독립 검토**: 다른 세션 또는 별도 검토 에이전트가 설계와 실행 코드를 확인한다.
   필요한 수정은 반영하고 검토한 버전을 기록한 뒤 실행한다.
3. **실행 및 결과 검토**: 설정·버전·입력 출처·집계 결과를 로컬에 보존하고, 별도 검토자가
   계산과 데이터 누수, 결과가 뒷받침하는 주장의 범위를 검증한다.
4. **결론 정리**: 전체 질문에 대해 채택·유지·기각·보류와 판단 근거, 한계 및 다음 결정
   사항을 정리해 사용자에게 먼저 설명한다. 개별 실험 번호나 중간 결과의 완료를 전체 연구의
   결론 또는 외부 게시 시점으로 임의 간주하지 않는다.
   승자가 없거나 채점 자료가 부족한 결과도 명시하며, 결과를 보고 판정 기준을 바꾸지 않는다.
5. **결론 이후 공유**: 사용자가 공유할 결론과 범위를 정해 Jira·GitLab 작성을 요청하면,
   그때 팀 저장소 `S15P21E106/AGENTS.md`, `docs/jira/README.md`와 Git 규칙에 따라 기록한다.
   결론에 필요한 문서, 설정, 실행·검증에 필요한 코드와
   소형 결과를 팀의 현재 `pipeline` 구조에 정리한다. 대용량 모델·Parquet은 Git에서 제외하고
   외부 위치·해시·공유 상태와 재현 가능한 범위를 기록한다. 개인 실험의 모든 중간 기록을
   일괄 복사하지 않는다. Jira Key가 포함된 별도 작업 브랜치에서 커밋·Push·develop 대상 MR을
   만들고, Jira에 MR·결론·검증 결과를 연결한다.

- 로컬 설계·실험·검증의 독립 검토는 계속한다. 이를 위해 중간 Jira나 MR을 자동 생성하지 않는다.
- 이전의 상시 외부쓰기 승인, 과거 실험 문서의 Jira-first 순서와 인계 완료 조건을 새로운
  게시 승인으로 사용하지 않는다. 과거 결과·봉인은 당시 기록으로 보존한다.
- 연구의 완료 범위는 사용자가 판단하려는 질문까지 포함한다. 분류 기반 추천의 효과를
   판단하는 업무를 분류 구성·후보 공급 진단에서 끝내거나 추천 품질 검증을 임의로 다음 업무로 분리하지 않는다.
- 추후 Jira를 작성할 때 SP는 파일 수·실험 횟수가 아니라 전체 범위의 설계 복잡도·불확실성·
  검증 난이도로 산정한다. 실제 배포나 공통 계약 변경은 연구·공유 요청에 포함된 것으로 해석하지 않는다.
- MR 생성 후에는 공유 완료·리뷰 대기를 구분해 보고한다. 승인과 병합은 사람이 하고,
  Jira 완료 상태는 팀의 병합 후 완료 절차를 따른다. 병합된 브랜치는 다음 실험에 재사용하지 않는다.

## 10. 데이터 관계 분석의 현재 범위

2026-09-09 최신 사용자 지시: **데이터 관계를 파악할 때 8개 분류를 전제로 삼지 않는다.**

- 현재 연구 범위는 `docs/recommendation/analysis-scope.md`를 따른다. 관계 분석의 입력·표본
  선정·채점·결론을 팝콘 맛 개수, 특정 군집 수, K-means 또는 미리 만든 장르 묶음에 맞추지 않는다.
- 복수 장르·키워드·인물 등 원래 속성을 유지한다. 영화나 사용자를 사전 고정된 취향 유형
  하나에 강제로 배정하지 않는다. 비교를 위한 정보 묶음의 개수도 8로 고정하지 않는다.
- 분석 결과를 사용자에게 어떻게 표현할지, 분류·군집을 도입할지와 그 개수는 관계 분석 후
  별도 판단한다. 기존 제품 표현 구상을 현재 분석의 제약으로 되가져오지 않는다.
- 과거 실험의 고정 코드·설정·결과·검토 봉인은 당시 기록으로 보존하고 현재 방침과 구분한다.
  새 분석을 한다면 최신 범위로 설계·독립 검토를 수행한다. 이 범위 조정만으로 운영 API·DB를 바꾸지 않는다.

## FILE: docs/recommendation/active-experiment.md
# 현재 로컬 추천 연구

상태: DRAFT — 개인 연구 업무 추적이며 제품 정책 계약이 아니다. 갱신2026-09-11.

## 현재 작업 방식

최신 사용자 지시에 따라 Jira 선등록 및 중간 실험별 자동 MR 생성을 중단했다.
**로컬 설계 → 실행 전 독립 검토 → 실험 → 독립 결과 검토 → 전체 질문에 대한 결론 정리**를
먼저 수행한다. 이후 사용자가 공유할 결론과 범위를 정해 요청할 때 Jira·GitLab에 기록한다.
개별 실험이 끝났다는 이유로 게시 시점을 임의로 정하지 않는다. 자료 수집은 로컬에서 진행하며,
사용자가 요청한 완료 연구 공유 업무만 아래 Jira·MR로 추적한다.
세부 규칙은 `AGENTS.md` 9절을 따른다.

2026-09-10 후속 사용자 요청으로 **현재 텍스트·결합·정책 연구의 진행 범위**를 Jira에 등록했다.
이는 아래 세 업무에 대한 명시적 요청이며 모든 중간 연구를 자동 게시하는 상시 방침으로 되돌리는 것은 아니다.

## 현재 결정 검토 — 모델 선택과 2편씩 추가 추천

2026-09-12 후속 **hybrid345 콘텐츠·ALS 결합/전이 비교와 독립 결과 감사(PASS)**를 완료했다.
[발표용 선정 정리](experiments/hybrid345/PRESENTATION-DECISION.md)에 무엇을 비교했고 무엇을 선택할지 정리했다.
구현 선정안은 ALS 지원 영역의 별점 오차를 유지하고 미지원 영역을 GBT120_s339로 보완하는 경로다.
Qwen 양의 혼합과 cold 대안은 사전 교체 기준을 통과하지 못했다. Router의 전체 MSE 점추정치는 낮지만
FM·GBT 대비 통계적 우위는 확인되지 않았다. 아래 과거 연구 단계의 후보 상태보다 이 최신 검토를 먼저 읽는다.
**프로젝트 모델 구성을 선택하기 위한 추가 모델 탐색은 종료해도 된다는 판단**이며,
팀의 공식 채택·구현·배포 완료나 한국 사용자 만족도 검증을 뜻하지 않는다.
빈 입력·미지원 입력과 2편씩 반환 등 실제 서비스 연결의 검증은 별도다. 기존 실험 봉인은 보존한다.

2026-09-11 최신 사용자 지시: **K-means·분류를 기반으로 맞춤/발견을 나누는 추천은 보류한다.**
첫2편을 보여주고 요청할 때마다2편씩 더 보여주는 흐름을 현재 검토 기준으로 삼는다.
과거 policy341의 맞춤2+발견1 연구 결과를 현재 서비스 정책의 채택안으로 적용하지 않는다.
장르·키워드 같은 원래 콘텐츠 속성을 모델 입력으로 쓰는 것까지 중단한다는 뜻은 아니다.

기존 실험과 관련 논문·공식 자료를 재검토해 설계상 부족한 검증, 남길 가치가 있는 제한된 실험,
MSE·Top2/4/6·각 추가2편·미지원 영화·비용을 고려한 모델 선택 기준을 정리한다.
이번 요청은 검토·선택 방안 마련이며 추가 학습이나 서비스 구현·외부 게시를 자동 시작하지 않는다.

[모델 선택 회고와 권고](model-selection-review-2026-09-11.md)에 기존 실험의 비교 조건,
MSE와Top2의 엇갈림, 전체 후보의 투표1개·10점 쏠림, 관련 논문과 제한된 후속 검증을 정리했다.
FM 계열을 주모델 후보로 두되 현재B나 고정ALS혼합을 그대로 채택하지 않는 제안이다.
2026-09-11 후속 요청으로 저장 예측의2편씩 재채점·전체 후보 노출·혼합 경계 진단3개 실행을 승인받았다.
[diagnostic342 결과](experiments/diagnostic342/RESULT.md)를 작성했다. 새 학습·추론 없이 관측 페이지와
전체 후보22,963,869행을 재사용했고, 페이지·노출·오차·경계 집계의 독립 검산을 통과했다.
기존FM의 투표1개·10점 노출은 개인입력186명 각372슬롯에서 첫2편342/다음2편303/그다음2편277이었다.
ALS25%혼합은 전체 첫2편2슬롯만 바꾸고 해당342슬롯 쏠림을 유지했다.
보정FM과GBT는 함께 검토할 후보로 남기며, FM우선이라는 이유로GBT를 제외하지 않는다.
GBT도 자연미지원 관측구간에서는 앞서지 않아 최종 승자를 확정하지 않는다.
진단3개를 완료했으며 새로운 모델·가중치·서비스 정책 선정이나 외부 게시는 수행하지 않았다.

2026-09-11 후속 사용자 요청으로 **설계 검증 후 새 학습과 최종 다중 지표 비교**를 승인받았다.
[research343 설계](experiments/research343/DESIGN.md)에 근거량 보정GBT 한 번과 동일입력
LightGBM 회귀/순위학습 두 번을 고정하고 **사전 검토·실제 학습3회·독립 수치 검산·최종 문서 검토를 완료했다.**
기존270명 중 보정90명/비교180명을 분리해 기존 모델도 같은180명에서
MSE·Top1/2/4/6·각2편·미지원·쏠림·비용을 비교했다.
[최종 비교](experiments/research343/RESULT.md)와 [독립 결과 검토](experiments/research343/result-review.json)를 남겼다.
입력 있는124명에서 순위 모델은 회귀 대조보다 보정 MSE가13.9% 높았고, 유효117명의
NDCG@2 개선은 입증하지 못했다. 근거량 보정GBT도 사전 두 지표의 개선을 입증하지 못했다.
보정FM(RH)의 별점 오차와 기존GBT의 관측 첫2편·전체 후보 노출 쏠림 사이에 절충이 남는다.
추가 모델 학습은 여기서 멈추며, 단일 승자나 새로운 서비스 모델을 확정하지 않는다.
이미 열어본 개발 표본이며 신규최종test나 한국2026 서비스 성능으로 부르지 않는다.
서비스 정책·Jira·GitLab·커밋·Push 변경은 수행하지 않았다.

2026-09-11 최신 후속 요청으로 **전체 실험·문헌을 다시 검토한 FM·GBT 최종 비교 설계**를 작성했다.
[읽기용 요약](plans/final-fm-gbt/README.md)과 [상세 설계](plans/final-fm-gbt/DESIGN.md)는
세 독립 검토자의 수정·재검토를 통과했다. 같은 RH230특징·499만 목표에서 FM150/300,
GBT60/120만 개발 비교하고 선정 설정에 추가2seed를 적용하는 최대8회 학습 설계다.
MSE·Top2를 ALL/C에서 함께 판단하며 새 사용자 미사용 이력 미입증·일부seed실패·보정N/A의
종료 규칙을 고정했다. 새 최종 test가 확보됐다고 가정하지 않는다.
**이번에는 설계만 완료했으며 실제 학습·새 평가 별점 개방은 하지 않았다.**
실행 코드·합성 검사·실제 입력 및 역할 감사는 후속 실행 전 검토 대상이다. 외부 게시도 수행하지 않았다.

2026-09-11 후속 **“해봐.”** 요청의 [final344 실행·검토](experiments/final344/README.md)를 완료했다.
원본 설계를 보존하고 코드·입력 검토 후 8회 학습, 모델·선택·최종 통계·전체 후보·문서·그림의 독립 검산을 통과했다.
같은499만 RH 입력에서 FM150·GBT120을 선택해 각각seed339/344/345를 비교했다. 보정MSE는 FM0.6890/GBT0.6793,
NDCG@2는 FM0.8373/GBT0.8359이며 ALL/C의 네98.75%CI 모두0을 포함해 단일 승자는 확인되지 않았다.
전체 첫2편 노출은 GBT가 덜 쏠렸지만 대부분의 추천에 평가 기록이 없어 만족도 우세로 해석하지 않는다.
[전체 결과](experiments/final344/RESULT.md)와 `result-review.json`에 수치·그림·봉인·독립 감사 근거를 남겼다.
8학습 모두 수치 검산PASS이며 엄격한 메모리기준은2PASS/6EXCEPTION(4~8KiB초과)이다. 실패·재시도·추가탐색은 없었다.
신규 확인 사용자의 미사용 이력은 입증되지 않아 DEVELOPMENT_ONLY이다. 서비스 채택·ALS혼합·K·분류 정책·외부 게시를 확정하지 않았다.

## 완료 이력 — 텍스트 → 정보 결합 → 이전 맞춤/발견 정책

2026-09-11 사용자 요청 **“자고올테니까3번까지쭉”**의 세 단계 실행과 독립 수치 검산을 완료했다.
[기준 모델 실행 명세](experiments/foundation340/EXECUTION.md)를 바탕으로
① 대중평점 근거량·목표시각 이전 학습입력의2×2 비교 → ② 개인반응 결합·ALS 보조 →
③ 맞춤2+발견1 정책을 독립 검토와 실제 채점까지 이어간다. 기초 FM 네 조건의 학습·관측/전체 후보 예측·
실제 통계 전수 검산을 완료했다. 사전 개선 기준을 통과한 조건이 없어 후속 비교 기준은 B로 유지한다.
R/RH의 대중 평점 보정은 투표1개·10점 영화 노출을 없앴지만 관측 후보 순위 품질 개선을 입증하지 않았다.
이는 B의 서비스 채택이 아니다. 2단계의 대중 반응 제거 FM·GBT·ALS 혼합도 사전 개선 기준을
통과하지 못해 S=B를 유지했다. 세 추가 학습과 전체 후보 약2,296만 행의 독립 예측 검산,
실제 지표·보정구간·선정 계산 검산을 완료했다.
3단계에서는 예측손실 제한을 둔 두 발견 정책이 기준을 통과했고, 사전 우선순위대로 P2_010을 남겼다.
관측 후보의 입력 있는158명에서 첫3편 평균별점은3.845992→3.841772, 장르 novelty는
0.517471→0.550321이다. 발견이 없는 경우도 포함한 수치다. 전체 카탈로그의 입력 있는186명에서는
첫3편558슬롯 모두 UNKNOWN이고 실제 발견은15명에게만 제공돼 서비스 채택 판단은 보류한다.
[최종 정리](experiments/final341/README.md)와 [편수·후보 공급별 표](experiments/final341/DETAILS.md)를
완성했다. 문서의345개 수치·상태 셀, 모집단·선정 문구·링크와 수정된 그림의 독립 검토도
[최종 PASS](experiments/final341/result-review.json)로 기록했다. 요청한 세 단계의 로컬 연구를 완료했다.
B는 저장 후 계측 오류가 나서 독립 검산한 모델을 재학습 없이 복구했으며 계측값은 UNKNOWN이다.
H·RH는 측정 최고 메모리12GiB+4096B 예외를 기록했다. 수치 검산과 자원 조건을 구분한다.
[정보 결합](experiments/combination340/EXECUTION.md), [맞춤·발견 정책](experiments/policy341/EXECUTION.md)의
설계·코드 사전 검토는 완료했다. 원본339·정제 실험 봉인은 보존한다. 중간 외부 게시를 자동 수행하지 않는다.

사용자 요청에 따른 [본문 의미 정제 비교](experiments/text339-clean/README.md)의 실제 학습·평가와
독립 수치 검산을 완료했다. 기존 text339 봉인을 보존하고 221편의 외부 설명 349구간을
보수적으로 삭제한 뒤 같은 조건의 FM 한 번을 추가 학습했다. 관측 후보 Top2 유효260명에서
NDCG는 기존 T3 0.8338→CLEAN 0.8313으로 소폭 낮아져 이번 정제 방식을 추천 개선안으로 채택하지 않는다.
입력 있는186명의 전체 후보 Top2 372슬롯은 여전히 모두 UNKNOWN이며, 그중368슬롯은
TMDB 투표1개·평균10점 영화였다. 두 모델 모두 측정 최고값12GiB+4096B 자원 예외를 기록했다.
이미 사용한 개발 표본·모델당 학습1회의 결과이며 텍스트 정제 전반의 인과효과나 서비스 성능을
확정하지 않는다. 원문 교정 이력과 사용자 중단 당시 미완성 파일을 보존하고 재준비했다.
위 문단은 정제 실험 완료 당시 기록이다. 이후 대중 평점·학습 입력·결합·정책 실험은 위 현황을 따른다.
이번 후속 연구의 Jira·GitLab 게시·커밋·Push는 수행하지 않았다.

[한눈에 보는 순서](plans/text-to-final-recommendation/README.md)와
[세부 설계](plans/text-to-final-recommendation/DESIGN.md)를 작성하고,
두 독립 검토자의 지적을 수정한 뒤 [범위 검토 PASS](plans/text-to-final-recommendation/design-review.json)를 받았다.

아래 Jira 상태는2026-09-10 등록 당시 기록이며 이번 로컬 완료에 맞춰 원격 상태를 변경하지 않았다.

1. [Jira339](https://ssafy.atlassian.net/browse/S15P21E106-339), 8SP, 진행 중:
   줄거리·Wikipedia 정제와 텍스트 추가 효과. 공통 평가 계약·텍스트 사용 판정부터 수행한다.
2. [Jira340](https://ssafy.atlassian.net/browse/S15P21E106-340), 8SP, 해야 할 일:
   개인 반응·콘텐츠 결합과 ALS 보조 기여. 표현 하나를 고정한 뒤 필요한 정보만 남긴다.
3. [Jira341](https://ssafy.atlassian.net/browse/S15P21E106-341), 13SP, 해야 할 일:
   맞춤·발견 Top-N 비교 및 최종 추천안. 선호 손실·새로움·후보 부족과 비용까지 정리한다.

선행 관계339→340→341, 담당 박찬형·기존 Epic124·활성 스프린트 배정을 재조회했다.
Top2/4/6과 공식 맞춤2+발견1 세트, 입력 상한H를 구분한다. 미평가는 UNKNOWN으로 유지하고
관측후보 순위 품질과 전체 카탈로그 공급 품질을 분리한다. 339의 로컬 실험·독립 검산·결론 정리를 완료했으며 새 MR은 만들지 않았다.
등록 당시 PASS는 로드맵/Jira 범위 검토였으며 실제 cohort·자원·embedding·통계·실행 코드 검토는 이후 단계별로 수행했다.
완료 결과·그림·판단 및 재현 근거를 정리한 뒤 선별 인계한다. 등록 기록은 [JIRA.md](plans/text-to-final-recommendation/JIRA.md)를 따른다.

339 실행 준비: [실행 명세](experiments/text339/EXECUTION.md)와 별도 `text339_*` 코드를 작성하고
사용자 역할·텍스트 소스·특징 수학·채점 코드의 독립 검토를 통과했다. 역할 감사에서 현재271명
모두 과거 개발에 사용된 것이 확인돼 이번 비교를 새 최종 test로 부르지 않는다.
원본 캐시와 같은 원문 T2/T3를 준비하고, 고정69편과 변경·복원140편의 의미를 독립 검토했다.
43편을 두 Wiki 조건에서 함께 제외한 실제 산출물 검산을 통과했다. GPU 표본 시험과
인코딩·재개 코드 검토 후 고정136542개 고유 본문을 약11분에 인코딩했다.
전체 영화행 연결·해시 검산 및 원래 GPU 배치의 독립 수치 재현을 통과했다.
공통 학습4997069행·평가93230행의 실제 산출물 검산 후 FM4조건의 순차 학습을 시작했다.
T0·T1·T2·T3 학습과 예측 봉인, 실제 네 모델 및 전체 카탈로그 수치 검산을 완료했다.
T3의 측정 최고 메모리는 설정12GiB보다4096바이트 높아 엄격한 최고 메모리 조건은 예외로 남겼다.
수치 검산 PASS와 자원 상한 준수를 구분한다. 최신 세부 실행 상태는
`outputs/recommendation-evidence/text339/run-progress.json`에 기록한다.
학습 입력0 비중82.34%와 최신 TMDB 날짜의 역사적 해석 한계는 [현재 연구 요약](experiments/text339/README.md)에 명시했다.
관측후보 Top2의 세 인접 비교가 모두 개선 기준을 통과하지 못해 이번 고정 설정에서는 T0를 유지한다.
실제 지표·분모·선정 계산과 보고 문구의 독립 검토를 완료했다. [최종 요약](experiments/text339/README.md)과
[결과 검토](experiments/text339/result-review.json)에 근거를 기록했다. T0의 서비스 채택을 뜻하지 않는다.
전체 카탈로그에서 입력 있는186명의 Top2 372슬롯은 네 조건 모두 채점 가능한 별점이 없었고,
사용한 TMDB 스냅샷의 투표1개·평균10점 영화가 그중93.01~99.73%를 차지했다.
339 완료 당시 후속340은 [대중 평점 근거량·학습 입력 구성](plans/text-to-final-recommendation/training-input-distribution.md)의
설계 검토부터 진행하기로 했다. 후속 실행·결론은 위 현황을 따른다. 이 진단만으로 인과적 원인이나
최소 투표 수 정책을 확정하지 않는다. 이번 로컬 결과의 외부 게시는 수행하지 않았고 기존 원문·실험 봉인은 보존한다.

과거 실험 문서와 봉인에 적힌 Jira-first 순서는 당시 실행 기록이며 현재의 외부 게시 승인이 아니다.
아래 종료는 중간 단계의 외부 게시 철회다. 기존 코드·분석·독립 검증 결과는 로컬에 보존한다.

## 완료 연구 공유 — 2026-09-10

사용자가 자료 수집 중 **완료된 실험·결과·장표·결론만 Jira → 브랜치 → MR로 공유**하도록 요청했다.
기존 295번 인계 구조를 따라 두 묶음을 게시했다. 수집은 Jira 범위에 포함하지 않았다.

- [Jira337](https://ssafy.atlassian.net/browse/S15P21E106-337), 8SP, 완료:
  REC046·표본 감사·REC047 모델 5종 및 학습량 비교.
  [MR !165](https://lab.ssafy.com/s15-bigdata-dist-sub1/S15P21E106/-/merge_requests/165),
  `docs/S15P21E106-337-model-comparison`, 커밋 `e250b6b4a7df7efbbeef1967a347a62431a3df29`.
- [Jira338](https://ssafy.atlassian.net/browse/S15P21E106-338), 8SP, 완료:
  목표 평점 이력을 가린 콘텐츠 예측·대중성 반응 비교.
  [MR !166](https://lab.ssafy.com/s15-bigdata-dist-sub1/S15P21E106/-/merge_requests/166),
  `docs/S15P21E106-338-cold-item-content`, 커밋 `ba09847301ae3b545fca8382bc74c276ce3c4931`.

연구 및 게시 완료 후 사용자가 두 MR을 develop에 병합했다. 병합 커밋은 각각 b371515e와 f4a4e272다.
Jira 완료와 source branch 삭제를 확인했다. MR !165의 삭제 브랜치 문서 링크는 병합 커밋으로 고쳤다.
구커밋 배포 차단으로 실패한186543과 달리 두 결과를 포함한 최신44b591f의186557은 배포까지 통과했다.
병합 후 `/claude-review`는 현재 열린 MR 전용 자동화에서 실행되지 않으며 독립 게시 검토와 별개다.
게시본 합성 검사21/8개, 원본/게시본 해시144/60건, 수치·분모·해석의 독립 게시 검토를 통과했다.
팀 `pipeline/docs`, `configs`, `artifacts`에 선별한 자료와 재현용 연구 코드를 보관했다.
원본 평점·사용자별 결과·Parquet·모델은 LOCAL_ONLY다. 원본의 LF·경로 표기 변환과 게시본 해시는
별도로 기록하며, EC2·전체 실험 즉시 재현이나 운영 계약 승격은 하지 않았다.
개인 연구의 변경된 RESULT 원문과 기존 미커밋 파일은 보존했다. 전체 개인 문서 일괄 게시 승인은 아니다.

## Wikipedia 줄거리 수집

2026-09-10 사용자가 Wikipedia 수집을 요청했다. [수집 설계](../research/wikipedia-collection/PLAN.md)에
따라 85,517편의 TMDB·IMDb ID에서 영어·한국어 문서를 연결하고 전수 수집을 완료했다.
실행 전 독립 검토 → 소규모 실제 수집·검토 → 전수 수집 → 최종 독립 검토를 완료했다.
80,322문서의 29개 필드와 원캐시 연결, 85,517편의 가용량·148개 층별 집계가 일치했다.
[최종 검토](../research/wikipedia-collection/result-review.json)는 수집·집계 PASS이며 본문의 의미 보존 전수 검증은 아니다.
영어 51,102편·한국어 6,534편, 중복 6,283편을 제외한 **51,353편(60.1%)**에서 비어 있지 않은
정제본문을 확보했다. [수집 결과](../research/wikipedia-collection/RESULT.md)에 누락·원문/정제본문
차이·개봉연대/원어/투표 수별 확보량을 기록했다. [내용 품질 검토](../research/wikipedia-collection/QUALITY.md)의
템플릿 의미 손실과 영화보다 넓은 문서 범위 사례를 별도로 보존했다. 확보 수는 모델 투입 승인 수가 아니며
모든 행의 `feature_ready`는 false다.
기존 TMDB overview는 영어 fallback을 포함하면 현재 85,517편 모두 존재하며,
29,058편은 ko-KR 원응답에서 overview가 비어 있지 않은 수다. 앞선 가용량 설명을 정정한다.
이번 작업은 데이터 확보이며 임베딩·새 모델 학습·운영 변경은 포함하지 않는다.

## 현재 판단할 핵심 질문

2026-09-09 사용자 목적 재확인: **ALS가 영화의 학습 평점 부족으로 직접 점수를 만들 수 없는
영화까지 추천하기 위해, 콘텐츠 단독 모델이 얼마나 유용한지를 판단한다.** REC047의 전체 평균과
ALS 유지 결론을 이 질문의 최종 해결로 간주하지 않는다.

후속 해석은 같은 실제 관측 별점에 대한 세 비교를 구분한다: ALS 지원 영화에서 단독 대안의
성능 격차, 영화 학습 평점 0건 구간에서 콘텐츠 단독 성능, ALS/FM/혼합을 같은 대상에서 비교한
혼합의 증감이다. ALS와 추천 목록이 일치하는 비율은 별도 유사성 지표이며 추천 정답으로 쓰지 않는다.
현재 혼합은 ALS 직접 점수가 없는 행에서 FM과 정확히 같으므로, 혼합 전체 성능으로 미지원 영화의
콘텐츠 능력을 입증할 수 없다. 후보 영화 미지원과 사용자 입력 부족도 구분한다.
서로 다른 영화·사용자에서 나온 지원/미지원 점수의 숫자가 비슷하다는 이유로 동급 판정을 하지 않는다.
새 학습이나 운영 전환 기준을 이 목적 정리만으로 확정하지 않는다. 기존 결과·봉인은 보존한다.

현재 후속 설계는 **[평가 이력을 아는 ALS와, 그 이력을 가린 콘텐츠 모델 비교](plans/als-reference-cold-item-comparison.md)**다.
같은 영화의 학습 평점은 참고 ALS에 남기고 FM·리지·GBT에서는 모두 제거한다. 같은 사용자의
동일 입력·동일 미래 관측에서 ALS 재현도와 실제 별점 품질을 나눠 비교한다. ALS 기본값과
혼합 모델은 이 단독 비교에 넣지 않는다.

2026-09-10 사용자가 실제 비교 실행을 요청했다. **[실행 명세](experiments/cold-item-content/EXECUTION.md)**에 따라
BASE / 근거량·수축 SUPPORT / 후보 대중 지표 CROWD_ITEM / 사용자 반응 CROWD_RESPONSE의
4조건을 FM·리지·GBT에서 비교했다. **[실제 결과와 판단](experiments/cold-item-content/RESULT.md)**에
12개 Spark 학습과 동일 대상 채점 결과를 정리했다. FM에 대중성 입력 분포·사용자 반응을 추가하면
입력10편의 순서 정확도가60.4%→66.5%로 개선됐고, 사전 보정 기준을 통과했다(유효71명).
입력30편도62.3%→70.8%였지만 개선은 미확정이다. 현재 근거량·수축 보정은 개선을 확인하지 못했고,
리지의 입력30편 MSE는0.8162→0.8427로 악화돼 보정 기준을 통과했다. ALS 비교48개는 모두
차이를 확정하지 못했다. 동급 판정이 아니며, 사용자 반응을 포함한 FM·GBT를 비교 후보로 유지하고
서비스 최종 모델·K·정책 채택은 보류한다. 두 모델 사이 우열 및 근거량 보정을 뺀 반응 조합은 미검증이다.

학습은39,859명·4,997,069개 평점이며12개 학습 함수 시간 합95.4분, 최대 메모리10.59GiB였다.
정답 값 없이 고정한 K10 대상은117명·461관측·312영화다. 준비 원자료·특징 감사,
12모델 native 예측 전수 검산과13열 최종 예측 봉인, 원본 목표별점482건·사용자 지표15,054행,
집계/짝비교와708개 구간의 독립 검산을 완료했다. [독립 검토 기록](experiments/cold-item-content/result-review.json)을 따른다.
기존 개발 정답과2026년 TMDB 수집본, 단일 영화 보류 분할의 결과다. 입력10편 평가자 중 과거30편
미만 사용자는2명뿐이며, 과거 시점 재현·현재 한국 서비스 품질·모든 취향 요인의 인과효과로 해석하지 않는다.
원자료와 모델은 로컬 보존한다. 완료 근거의 선별 공유 상태는 위 Jira338 / MR !166을 따른다.

## 최근 로컬 근거

[REC047 전체 이력·학습량 비교](experiments/rec-ev-047/RESULT.md)를 완료했다.
동일한 시간·입력 규칙과 평가 대상에서 학습 사용자와 전체 과거 평점을 중첩25/50/100%로
늘렸다. 실제 학습24개, 원본·전체특징·예측·최종정답·사용자지표·집계CI·결론 독립 검산 PASS다.
최종100% 학습은39,859명·6,261,195평점이며, 고유평가271명·관측별점18,960건을 사용했다.
K0/1/5/10/30의 평가자는271/187/186/186/183명이고, 순서 지표의 유효 인원은 별도로 집계했다.

사전 학습량25→100% MSE대조10개 모두 보정구간이0을 포함해 개선을 확인하지 못했다.
입력30편의 ALS 순서정확도62.6%는 FM·리지·GBT보다 높았지만 ALS+FM과 차이는 미확정이다.
같은 조건에서 MSE우위는 리지 대비만 확인했다. 학습지원0 영화에서는 활동집단별 MSE
순서가 뒤집혀, 전체평균만으로 콘텐츠가 항상 낫다고 판단할 수 없다. ALS를 유력 비교 후보로
유지하며 부족한 입력·영화지원의 보완은 별도 가설로 남긴다. 서비스 최종모델·전환K는 확정하지 않는다.

학습평가는 사용자 역할을 분리했고, 평가의 저활동1~29편 사용자는4명뿐이다. 최종100% 지도학습
목표의82.3%는K0이고 입력이 있는 목표는1,109,621건이다. 학습량을 모두 개인화 학습량으로
부르지 않는다. 현재TMDB·과거MovieLens·기존개발자료의 결과이며 한국 서비스 대표성의 보장은 아니다.
데스크탑 Spark local[4]에서 학습시간 합약88.0분, 최대컨테이너메모리10.28GiB였다.
[결과 감사](experiments/rec-ev-047/result-review.json)와 소형CSV·그림·재현코드를 로컬에 보존한다.

사용자 요청에 따른 [REC046 데이터 구성 감사](experiments/rec-ev-046/data-audit/RESULT.md)를 완료했다.
학습·검증·평가의 사용자 활동량과 영화 지원량, 자격 선별 및 학습 표본 축소의 영향을 확인한다.
독립 사전 검토 2건과 합성 4검사 후 기존 모델·사용자·설정을 유지해 재집계했고 결과 감사도 PASS다.
원본에서는 사용자/영화/시간 세 열만 집계하고 새 별점·학습·튜닝·외부 게시를 하지 않는다.
이번 검증·평가자 모두 과거 30편 이상이라는 한계를 단순 K=1 실험으로 대체해 설명하지 않는다.
과거 300편 이상 사용자 비중은 학습 약 11%·검증 53.3%·평가 62.3%였다. 동일 학습자의
전체 허용 과거 평점 중 실제 R은 약 13~14%이고, 평가의 감독 블록은 약 89~90%가 OOV만 있었다.
활동량×영화 지원량 24개 평가 셀 중 12개는 자료 없음, 4개는 사용자 30명 미만이었다.
기존 점수는 현재 표본·학습량·특징 표현의 결과로 보존한다. 그 순서로 서비스 후보를 좁히는
판단은 보류하고 사용자 활동량·입력 생성·미래 목표 기간·학습량·표현 지원을 먼저 정리한다.

2026-09-09 사용자 요청으로 **[REC046 추천 모델 5종 비교](experiments/rec-ev-046/README.md)**를
설계·독립 검토·실행·결과 검토하는 로컬 루프를 완료했다. ALS, FM, ALS+FM, 공유 교차 리지,
Spark GBT를 같은 허용 자료에서 비교한다. 학습에 없는 영화가 입력/후보에 포함될 때도 다룬다.
[결과 보고서](experiments/rec-ev-046/RESULT.md): 데스크탑 단독으로 24개 Spark 학습을 완료하고,
고유 평가자 300명·48,326개 별점을 채점했다. 별점 오차에서는 GBT와 ALS+FM이 유력했으나
선호 순서까지 고려한 단일 승자는 없다. 입력 10/30편에서 ALS 보조가 FM의 MSE를
2.70%/3.24% 줄였지만 선호 순서 개선은 미확정이다. 리지는 입력 없이도 순서 점수가 높아
이를 개인화 효과로 단정하지 않는다. 미학습 영화의 ALS 기본 점수도 영화 구별 능력과 구분한다.
현재 TMDB·과거 MovieLens·회차당 12,000명 학습의 탐색 비교다. 이전 대규모 ALS의 기각이나
현재 한국 서비스의 품질 보증은 아니다. 실행 전 검토와 독립 결과 감사 2건 모두 PASS.
모델·원자료·봉인은 로컬 보존, 서비스 최종 모델·K는 보류한다. 완료 근거의 선별 공유는 위 Jira337 / MR !165에 기록했다.

현재 사용자 요청은 **한 달 팀 프로젝트에서 사용할 서비스 추천 시스템 전체의 재설계**다.
대규모 사용자별 채점 자료는 현재 MovieLens뿐이며 자체 자료는 프로젝트 전체에서 매우 적다는 조건이다.
과거 MovieLens·현재 TMDB/KOBIS·소량의 본인 별점을 어떻게 연결해 현재 한국 사용자에게 추천할지 답해야 한다.
프로젝트 이후의 대규모 자체 데이터 축적을 해결 조건으로 두지 않는다.
[팀 공유용 문제 정의 문서](2026-korea-data-and-evaluation-redesign.md)에 데이터별 역할,
ALS 사용 조건, OTT 접근 맥락, 채점의 신뢰 범위와 설계할 항목을 정리했다.
문서는 DRAFT이며 모델·입력량·군집·운영 정책을 확정하지 않는다. 다음 구현 범위는 재설계 논의에 맞춰 정한다.

REC046 이전 사용자 요청에 따라 **MovieLens 개봉연도별 관측 분포 점검**을 먼저 완료했다.
[전수 결과와 그림](../research/movielens-release-distribution/REPORT.md),
[모든 개봉연도 표](../research/movielens-release-distribution/BY_YEAR.md)를 먼저 확인한다.
87,585편·32,000,204개 별점 기록·200,948명 집계와 독립 전수 검산을 완료했다.
1990·2000년대 영화는 카탈로그의27.06%/평가의61.47%, 평가 상위1% 영화는 전체 평가의55.76%다.
REC045 최근작의 상대 비중은 원본보다 높지만2020~2023년작의 평균 가중치는1.57%,2023년작은0.14%다.
2024~2026년작의 관측 평점은 이 자료에 없다. 이 분포 감사에서는 모델 학습·별점 채점을 하지 않았다.
**기존 전체 성능 개선을 최신작·평가가 적은 영화·현재 서비스의 개선으로 확대하지 않는다.**
후속 성능 실험 전에 개봉 시기×학습 평가 지원량별 검증 범위와 목표 영화 구성을 먼저 검토한다.
집중도는 자료 내부의 기술통계이며 실제 시장과의 편향 크기나 새로운 가중치 정책을 확정한 것은 아니다.

현재 기준은 [분류 수를 전제하지 않는 데이터 관계 분석](analysis-scope.md)이다.
최신 사용자 지시에 따라 **8개 분류·팝콘 맛·K-means를 관계 분석의 전제에서 제외한다.**
복수 장르·인물·국가 등 원래 속성을 유지하며, 정보 묶음의 개수도 고정하지 않는다.
REC045의 ‘8개 블록’은 입력 필드 묶음 수였고 영화8군집이나 맛 ID를 사용한 것이 아니다.
분류·표현 방식과 개수는 데이터의 관계를 확인한 뒤 별도 판단한다.

앞선 [다각도 데이터 탐색·안정성 분석안](plans/multidimensional-preference-analysis.md)은
필드 목록과 이전 탐색 순서의 참고 자료이며, 거기에 남은8맛 문구는 현재 분석 조건이 아니다.
최신 사용자 지시대로 자료 전반의 분포와 정보 간 관계를 먼저 보고, 적은 입력/반복 근거와
서로 다른 사용자·영화 집단에서도 관계가 유지되는지 확인한 뒤 모델 입력을 결정한다.
앞선 [메타데이터 지원 범위](plans/multidimensional-feature-support.json)를 바탕으로
[REC045 복합 정보 설계](experiments/rec-ev-045/README.md)와
[시간 진단](experiments/rec-ev-045/temporal-design.md)을 작성하고 독립 검토·실행·결과 감사를 완료했다.
전체 85,517개 기존 TMDB 캐시의 해시를 검증하고 추가 필드 전처리를 완료했다.
감독/상위5배우의 O→E 연결은8.22%/31.53%, 입력에서 같은 인물2편 이상 조건이면1.46%/6.01%다.
이는 평점 값을 읽지 않은 자료 가용성 통계이며 새로운 개인 취향·성능 결과가 아니다.
[REC045 결과](experiments/rec-ev-045/RESULT.md): 고유3,819명·사용자재분할3회에서 복합 개인 보정의
공통 보정 대비MSE 감소는K5/10/30에서0.87%/1.80%/4.51%다. K30 장르 개인 보정 대비2.49% 추가 감소.
장르·대중 평가 특성의 기여가 상대적으로 크고, 키워드·인물·제작사/시리즈는 작은 보조 기여가 있었다.
국가/언어·연대/길이의 추가 기여와 학습평가50개 미만 영화의 개선은 미확정이다.
고유300명의2019 시간 진단에서 복합은 장르 개인 보정보다2.44% 낮은MSE였지만 공통 보정만
추가한 단계는 기본보다 악화됐다. 인물×연대의 시간 추가 이득은 미확정이다.
원자료·전체 지표·집단·독립수식·고정밀 수치 민감도 감사PASS. 이미 사용한 개발 자료와 현재
정적TMDB를 조건으로 한 관계 진단이며, 인과관계·ALS우열·실서비스추천·최종K의 결론은 아니다.
현재 분석에 분류 수를 요구하지 않는다. 공유는 사용자가 범위를 정해 요청할 때 진행한다.

아래 REC044 이전의8분류 관련 내용은 과거 실험 이력이며 후속 분석의 전제는 아니다.

[REC044 설계](experiments/rec-ev-044/README.md): 영화의 대중 평가와 개인의 별점 습관을
감안한 뒤 장르·키워드·현재8분류의 개인별 편차가 다른 영화에서도 반복되는지 분석한다.
동일 O30/E와 학습 사용자 전용 영화 평균으로 다섯 표현을 공통 고정 진단기에 비교한다.
[결과](experiments/rec-ev-044/RESULT.md): 기준 대비 MSE 감소는 장르2.04%, 키워드0.55%,
결합2.20%다. 현재 규칙8/KM8의 개선은 미확인, 결합의 장르 단독 대비 이득도 미판정이다.
장르별 입력/가린별점 편차 표와 타인 프로필 대조를 기술통계로 남겼다.
사전 두 독립 검토 PASS·합성13검사 후 실행32.547초. 결과 독립 검산과 읽기 전용 재검증을
완료했다. 이전 개발 표본 재사용이며 실서비스·인과관계·ALS 우열의 결론은 아니다.
Jira·GitLab 게시 대상이 아니며 외부8맛·최종K·추천/발견 정책은 변경하지 않았다.

[REC043 설계](experiments/rec-ev-043/README.md): 추천 정책 전에 분류와 사용자 별점의 관계를 분석했다.
동일 O30/E에서 개선8장르규칙·8장르KM·원본18장르를 비교했다. ALS지원 필터와 추천 목록은 사용하지 않았다.
사전 독립 검토3명·합성15검사 후 실제 분석과 독립 결과 검산3명까지 완료했다.
[REC043 결과](experiments/rec-ev-043/RESULT.md): 현재 규칙8/KM8의 개선 미확인,
TMDB18복수 장르는 영화 평균 보정 기준 대비 MSE1.70% 감소. 실서비스/표현 간 우열 검증은 아니다.
팀 재실행 집계 완전 일치, 새15+기존24=39검사 및 Ruff통과.

앞선 [REC042 결과](experiments/rec-ev-042/RESULT.md)도 보존한다. 분류를 붙인 앞2편의 개선 미확인,
발견교체의 관측선호손실을 기록했으며, ALS 비교 기준 유지·분류와 발견 정책 채택 보류 결과다.

## 외부 게시 철회 상태

- [Jira300](https://ssafy.atlassian.net/browse/S15P21E106-300): 사용자 요청에 따른 게시 철회로 종료.
  프로젝트에 취소 상태가 없어 완료 상태를 사용했고 본문·댓글226200에 사유를 명시했다.
  [MR !129](https://lab.ssafy.com/s15-bigdata-dist-sub1/S15P21E106/-/merge_requests/129): Closed 확인, 게시 철회 사유 댓글2776554 기록. 병합하지 않았다.
- [Jira309](https://ssafy.atlassian.net/browse/S15P21E106-309): 같은 사유로 종료, 본문·댓글226201과 상태 재조회 확인.
  [MR !130](https://lab.ssafy.com/s15-bigdata-dist-sub1/S15P21E106/-/merge_requests/130): Closed 확인, 게시 철회 사유 기록. 병합하지 않았다.
- REC043 팀 인계 worktree `C:/higher/projects/S15P21E106-309-preference-structure`,
  브랜치 `feat/S15P21E106-309-preference-structure`, 커밋 `bc175c67c50974fadd07f6d9f6397342368af887`는 보존한다.
  REC042 커밋77e3e28와 관련 worktree도 삭제하거나 되돌리지 않는다.
- 이미 병합된 Jira295 / [MR !126](https://lab.ssafy.com/s15-bigdata-dist-sub1/S15P21E106/-/merge_requests/126)는 기존 기준선·입력·콘텐츠 근거의 이력으로 유지한다.
- 별도 팀 전처리 개발 업무 Jira118 / MR !127은 이번 추천 연구 게시 철회의 범위에 포함하지 않았다.

사용자별·대용량 원자료는 Git 제외, 외부 공유 상태는 LOCAL_ONLY다. EC2·공식 맛 매핑·전환K·운영 API/DB를 연구 결과만으로 변경하지 않는다.
과거 실험 결과·봉인은 수정하지 않는다. 로컬 결론 정리가 먼저이며 게시와 병합은 별도 상태로 구분한다.

## FILE: docs/recommendation/experiments/hybrid345/PRESENTATION-DECISION.md
# 추천 모델 선정 — 발표용 정리

상태: **DRAFT · 검증된 개발 실험에 근거한 구현 선정안**  
범위: final344와 hybrid345의 로컬 결과. 팀의 공식 채택·구현·배포 완료 기록은 아니다.

## 발표의 핵심 문장

> ALS, FM, GBT, 콘텐츠 유사도와 결합 방식을 같은 평가 조건에서 비교했습니다.
> ALS가 점수를 계산할 수 있는 영화에서는 ALS의 별점 제곱오차가 가장 낮았습니다.
> ALS가 계산하지 못하는 영화에서는 FM·GBT가 유력했고, 콘텐츠 및 ALS 벡터 전이 방식은
> 사전에 정한 교체 기준을 통과하지 못했습니다.
> 따라서 별점 오차와 추천 가능한 영화 범위를 함께 고려해, ALS가 계산 가능한 경우에는
> ALS를 유지하고 미지원 경우에는 GBT로 보완하는 구현안을 선정했습니다.
> 이 선택은 여러 지표의 절충에 따른 설계 판단이며, 모든 지표에서 가장 좋은 모델이라는 뜻은 아닙니다.

현재는 **“구현안을 선정했다”**가 정확하다. 팀이 이 구성을 확정하면
**“추천 구조로 채택했다”**로 표현할 수 있다. 서비스 적용 완료는 실제 구현·연동 확인 후에 쓴다.

## 무엇을 비교했고, 왜 선택했는가

| 판단할 질문 | 비교와 결과 | 선정안 |
| --- | --- | --- |
| ALS 지원 영화에서 무엇을 유지할까? | MSE는 ALS **0.6938**, FM **0.7198**, GBT **0.7143**. 반면 NDCG@2는 ALS **0.8333**, FM **0.8363**, GBT **0.8428** | 별점 오차를 근거로 ALS 유지. Top2까지 ALS가 최고라고 주장하지 않음 |
| ALS에 콘텐츠를 섞을까? | Qwen 비중 0·10·25·50% 비교. 양의 비중은 MSE·NDCG@2 동시 개선 기준을 통과하지 못해 **0% 선택** | 이번 Qwen 점수 혼합 제외 |
| ALS가 모르는 영화는 어떻게 계산할까? | 학습에서 제외한 영화 C에서 MSE: 구조 콘텐츠 **0.8996**, E5 **0.8566**, Qwen **0.8586**, ALS-C2F **0.8241**, FM **0.7486**, GBT **0.7287** | 사전 교체 기준을 통과한 대안이 없어 기존 **GBT120_s339 유지** |
| 두 경로를 합치면 전체 성능도 더 좋을까? | 전체 관측 후보 MSE: Router **0.6645**, FM **0.6890**, GBT **0.6793**. NDCG@2: **0.8340 / 0.8373 / 0.8359**. 사전 대조 신뢰구간은 모두 0 포함 | 역할 분리 경로로 사용 가능한 후보. GBT 단독보다 우수하거나 동등하다고 입증된 것은 아님 |

표의 FM·GBT는 각각 세 학습 seed의 **사용자별 지표 평균**이다. 앙상블 예측이 아니다.
ALS는 기존 단일 학습, Router는 사전에 고정한 seed339 경로다. GBT 한 seed의 유리한 값만
FM 세 seed 평균과 섞어 모델군 승패를 주장하지 않는다.

## 발표 숫자를 읽는 조건

- 주 조건은 입력 최대 10편(cap10), 실제 입력이 있는 사용자다. 10편 입력을 서비스 필수 조건으로 선정한 실험이 아니다.
- MSE는 별도 사용자로 보정한 예측의 제곱오차이며 낮을수록 좋다. NDCG@2는 관측 별점이 있는 후보 안에서의 순위 지표이며 높을수록 좋다. 정확도 백분율이 아니다.
- 사용자 분모는 MSE / NDCG@2 기준으로 지원 영화 **122 / 109명**, 제외 영화 C **97 / 72명**, 전체 **124 / 117명**이다. 서로 다른 영화 집합의 수치를 직접 빼서 개선율을 만들지 않는다.
- C는 영화의 학습 평점을 의도적으로 모두 제외한 집합이다. ALS는 그 관측 목표 **624행 중 0행**을 계산했고, 기본값을 넣어 비교하지 않았다. 원래 평점이 없는 영화나 2026년 신작 전체의 품질을 이 결과로 보증하지 않는다.

## 질문에 답할 때

**“검증된 실험인가?”**

그렇다. 설계·실행 코드의 독립 검토, 사용자 역할과 영화 제외 조건 확인, 입력·모델·결과 파일의 해시 확인,
원본에서의 독립 재계산을 거쳤다. hybrid345 결과 감사는 PASS이고, 사전 14개 대조를 각각 사용자 bootstrap
20,000회로 계산했다. Top1·2·4·6, 2편 단위 페이지, 저평점 노출과 전체 카탈로그 쏠림도 확인했다.
이는 계산과 명시한 개발 비교의 검증이다. 과거 실험에서 사용했던 사용자와 영화를 재사용했으므로
완전히 미사용한 최종 시험 자료에서 일반화 성능을 확인한 것은 아니다.

**“GBT나 Router가 통계적으로 가장 좋은가?”**

그렇게 확인되지 않았다. GBT의 사전 유지 결정과 전체 단일 승자 판정은 다르다.
ALS는 지원 영화 MSE, GBT는 일부 Top2 지표, FM은 일부 순위·계산비용 측면에 장점이 있다.
Router도 GBT 단독보다 노출이 더 쏠렸다. 전체 Top2 고유 영화는 Router **78편**, GBT_s339 **133편**이다.
이번 선정 이유는 오차와 지원 범위의 절충이다.

**“추가 실험을 해야 하는가?”**

이번 프로젝트에서 사용할 모델 구성을 선택하기 위한 **새 모델 탐색·추가 가중치 탐색은 여기서 종료해도 된다.**
동일 개발 자료에서 실험을 더 늘리는 것을 서비스 적합성의 증명으로 삼지 않는다.
남은 작업은 선택한 모델의 실제 카탈로그·사용자 입력 연결, 빈 입력·지원 없는 입력 처리,
2편씩 중복 없이 반환되는 동작과 실행 환경의 비용·응답 확인이다. 현재 실험만으로 이 연동이 완료됐다고 말하지 않는다.

“2026년 한국 사용자에게 더 좋은 추천” 또는 “미사용 자료에서 통계적으로 우월한 모델”이라고 주장하려면
각 주장에 맞는 새 평가가 필요하다. 이것을 현재 한 달 프로젝트의 모델 선정 완료 조건으로 추가하지 않는다.

## 한계 슬라이드에 넣을 한 문장

> MovieLens 개발 자료로 오차와 순위, 미지원 영화의 점수 계산을 검증했으며,
> 2026년 한국 사용자의 만족도는 별도 검증 범위입니다.

전체 카탈로그 Top2에서 GBT_s339는 **245/248개 노출(98.79%)**, Router는 **248/248개 노출(100%)**에
해당 사용자의 관측 별점이 없었다. 이는 오답률이 아니며, 전체 카탈로그 만족도 점수로 바꿀 수 없다.

## 근거

- [ALS·FM·GBT 같은 영화 비교](../final344/FINAL-DECISION.md)
- [Qwen·ALS-C2F·결합·Router 전체 결과](RESULT.md)
- [사전 고정 설계](DESIGN.md)
- 결과 및 선정 원본: `outputs/recommendation-evidence/hybrid345/summary.csv`, `selection.json`, `primary-contrasts.csv`, `catalog-summary.csv`
- 독립 결과 감사: `outputs/recommendation-evidence/hybrid345/result-audit.json` — **PASS / result_report=MATCH**

이 문서는 봉인된 실험 원문을 변경하지 않고 발표 해석을 정리한 문서다.

