Status: DRAFT, bounded diagnostic design; independent pre-execution review required.

Use the finalized r2 frozen technical exemplar exactly, without changing its policy,
representatives, centroids, preprocessing, content axes, runtime rules or quality C/m.
The diagnostic uses the uid-ascending first20 check/verification cap10 users whose
profile is VALID under the frozen policy's content space and profile mode. It does
not require changed results, sufficient candidate supply or observed future labels.

The three scenarios are baseline, append, and append followed by tombstone deletion.
Append uses the exact three rows from dv2_freeze.py: a first-row metadata clone with
fresh service/TMDB IDs, a second-row raw-null content fixture, and a third-row OOV
content fixture. Other metadata fields remain inherited. Deletion uses the exact
final invariance medoid/history ID set, independently reconstructed from the first
eight positive frozen medoid IDs and first serving-parity user's first history ID.
The history fixture user can differ from the diagnostic's first20; its actual
history/viewed overlap is recorded. Tombstones keep past vectors and group labels.

For every user/scenario, independently reconstruct each legal underseen group's
full eligible unseen E_u pool in frozen Q/v/service-ID order and its quota prefix.
Verify every prefix against the production retrieval Engine. Save full pool IDs,
prefix IDs, counts and mean profile similarities. Retrieve the frozen selected
group policy with no predictor and save its actual candidate IDs and group order.
Compare all three phase pairs for pool/prefix/candidate overlap, signed similarity
deltas, changed-order counts and vanished/new groups. A zero-drift result remains
a valid result. Full pool means and Q-prefix means are reported separately.

The inputs are sealed root artifacts, read-only. Baseline vectors are the prepared
arrays whose exact material hashes match the frozen bundle; baseline labels are
the final assignments whose full-catalog frozen classification was independently
validated by the final invariance stage. Only three appended rows are transformed.
Frozen quality is recomputed independently and compared exactly to prepared Q and
global order. Full final/prepare inventories, source seal pins, reviewed code and
bundle material hashes are checked before and after. No runtime predictor module
is imported, no trained prediction is computed, no refit or policy selection occurs.
All1350 numeric future ratings arrays are redacted in source bytes before JSON
parsing; only explicit past-input fields are retained. Reading those source bytes
is recorded and is distinct from parsing their future rating values.

Bounds:20 users,3 scenarios,2 numerical threads,900seconds,8GiB process RSS,
2GiB minimum host free memory and100MiB new output. A fresh isolated output path
is required; partial or previous output is never overwritten. A PASS code ledger
must match the exact fingerprint, and execution also requires the explicit final
seal SHA256. Fingerprint-only mode does not run the diagnostic. Results and a
complete output seal remain in this worktree for independent result review.
